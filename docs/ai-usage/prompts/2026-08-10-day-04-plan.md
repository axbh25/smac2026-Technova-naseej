Assume that all of Day 3 steps are complete and verified for the Naseej Flutter app.

The project is part of the SMAC 2026 Project Development and the team name is Technova.

Project name:
Naseej

Local project path:
C:\dev\smac2026\naseej

GitHub repository:
axbh25/smac2026-Technova-naseej

Day 3 is complete.

The app currently supports:

Welcome
→ Profile Setup
→ locally saved profile
→ Home

The app also already supports:

- English and Arabic localization
- RTL
- persistent language selection
- persistent family profile
- nickname
- family role
- empty Home state
- Teach a Skill and Learn a Skill buttons
- automated tests
- Android emulator verification

For Day 4, I want to implement the first real skill-sharing flow:

Home
→ Teach a Skill
→ create a skill draft
→ save the draft locally
→ return to Home
→ show the saved draft
→ reopen and continue editing the draft

The work should remain local-only.

I am a beginner, so give me extremely clear, chronological, baby-step instructions.

Tell me:

- exactly what folder to open
- exactly what file to create
- exactly what code to paste
- whether I should replace a complete file or only a section
- exactly what PowerShell commands to run
- how to verify each important step

Do not skip intermediate verification.

==================================================
DAY 4 MAIN GOAL
==================================================

By the end of Day 4, Naseej should support a locally persisted Teach a Skill draft.

The user should be able to:

1. Open Teach a Skill from Home.
2. See their existing local profile as the teacher.
3. Enter the learner's nickname.
4. Choose the learner's family role.
5. Choose a skill category.
6. Enter an explanation of the skill.
7. Save only when all required information is valid.
8. Save the draft locally on the phone.
9. Return to Home.
10. See a summary of the saved draft.
11. Reopen the saved draft.
12. Continue editing the previous values.
13. Restart the app and still recover the saved draft.
14. Use the complete flow in both English and Arabic.
15. Keep RTL working.
16. Keep all Day 2 and Day 3 functionality working.

Do NOT build the Learn a Skill flow yet.

==================================================
GIT WORKFLOW
==================================================

Start from the completed Day 3 main branch.

Create and work on:

feature/day-04-skill-draft

Give me the exact commands for:

git switch main
git pull --ff-only origin main
git switch -c feature/day-04-skill-draft
git push -u origin feature/day-04-skill-draft
git branch --show-current

From Day 3 onward, Abdullah performs all VS Code, Flutter, and Git development.

Do NOT require Shoug to run VS Code, Flutter, PowerShell, or Git commands.

==================================================
DAY 4 FLOW
==================================================

The intended Day 4 flow is:

Home
→ Teach a Skill
→ Skill Draft screen
→ Save Draft
→ Home with saved draft
→ Continue Draft
→ Skill Draft screen populated with saved values

The Learn a Skill button should remain a future feature and may continue to show an honest temporary "coming soon" message.

Do not create fake functionality for features that are not implemented yet.

==================================================
FIGMA
==================================================

Use the existing Naseej Figma design system.

Do not redesign the existing:

- Welcome screens
- Profile Setup screens
- Home screens

Create the Day 4 Teach a Skill design in both English and Arabic.

Continue using the established reference screen:

412 × 892

Use the same:

- background
- colors
- typography
- spacing
- buttons
- field styling
- cards
- 48 × 48 touch targets
- RTL conventions

The Teach a Skill screen should include:

- app bar/header
- back navigation
- language control
- title
- teacher summary
- learner nickname field
- learner role selection
- skill category selection
- explanation field
- Save Draft button

Use reusable components wherever appropriate.

Do not detach existing component instances unnecessarily.

==================================================
TEACHER SECTION
==================================================

The teacher must come from the already saved family profile.

Do not ask the user to re-enter their teacher nickname or role.

Display a compact teacher summary containing:

- teacher nickname
- teacher role
- role icon
- message explaining that the current local profile is teaching the skill

Example concept:

Fatima — Grandparent

Arabic should use the localized equivalent.

The teacher information must come from the existing FamilyProfile.

==================================================
LEARNER SECTION
==================================================

Allow the user to enter:

Learner nickname

Maximum sensible nickname length:

24 characters

Use a TextField with a stable test key:

learner_nickname_field

The learner must also have one family role.

Reuse the existing family roles:

Grandparent
Parent
Teen
Child

and their Arabic translations.

Reuse the existing role UI/component logic rather than creating a second conflicting role system.

Test keys should follow:

learner_role_grandparent
learner_role_parent
learner_role_teen
learner_role_child

==================================================
SKILL CATEGORIES
==================================================

Create exactly four Day 4 skill categories:

heritage
everyday
digital
familyCare

Create a SkillCategory enum.

The enum should support converting stored string values back to a valid category.

Unknown stored categories must be rejected safely.

Create localized labels for all four categories.

Create an appropriate reusable category choice card.

Suggested structure:

lib/features/skill/presentation/widgets/
    skill_category_choice_card.dart

Use:

- Default state
- Selected state
- existing Naseej colors
- existing borders
- existing radius
- clear icon
- clear label

Use stable widget keys:

category_heritage
category_everyday
category_digital
category_familyCare

The category cards must not overflow at the 412 × 892 reference size.

English and Arabic labels must fit correctly.

==================================================
SKILL DRAFT DOMAIN MODEL
==================================================

Create:

lib/features/skill/domain/skill_draft.dart

The SkillDraft must contain:

teacherNickname
teacherRole
learnerNickname
learnerRole
category
explanation

Use:

FamilyRole

for teacherRole and learnerRole.

Use:

SkillCategory

for category.

The model must support:

toJsonString()

and:

fromJsonString()

for local persistence.

fromJsonString must safely return null for:

- null input
- empty input
- damaged JSON
- missing/wrong field types
- unknown family roles
- unknown skill categories
- invalid explanation lengths

Implement equality and hashCode so model round-trip tests can compare restored objects.

==================================================
EXPLANATION VALIDATION
==================================================

The explanation must have:

Minimum length:
20 characters

Maximum length:
600 characters

Create constants on SkillDraft for these limits.

The Save Draft button must remain disabled if the explanation is shorter than 20 characters.

It must also reject explanations longer than 600 characters.

The explanation field should support multiline input.

Use:

minLines: 5
maxLines: 7

or an equivalent layout appropriate to the design.

Use a stable key:

explanation_field

==================================================
SAVE DRAFT VALIDATION
==================================================

The Save Draft button must remain disabled until ALL of these are present:

1. learner nickname is not empty
2. learner role is selected
3. skill category is selected
4. explanation has at least 20 characters
5. explanation does not exceed 600 characters
6. no save operation is already in progress

Use the stable key:

save_draft_button

The button should visually use the existing Naseej primary button styling.

Do not fake an enabled state.

==================================================
LOCAL STORAGE
==================================================

Extend the existing AppStorage abstraction.

Add:

Future<String?> readSkillDraftJson();

Future<void> writeSkillDraftJson(String skillDraftJson);

Future<void> clearSkillDraft();

Update the SharedPreferences implementation accordingly.

Use a dedicated SharedPreferences key for the saved skill draft.

Do not store the draft in a global variable only.

The saved draft must survive a real app restart.

Continue keeping all storage local to the device.

Do not add Firebase.

Do not add a backend.

Do not add authentication.

==================================================
FAKE STORAGE FOR TESTS
==================================================

Update:

test/support/fake_app_storage.dart

because AppStorage now has three new methods.

Add:

String? skillDraftJson;

and implementations for:

readSkillDraftJson()
writeSkillDraftJson()
clearSkillDraft()

Allow FakeAppStorage to optionally receive skillDraftJson in its constructor.

The fake must fully implement AppStorage so flutter analyze reports no missing abstract members.

==================================================
APP CONTROLLER
==================================================

Extend AppController to manage the saved skill draft.

Add:

SkillDraft? _skillDraft;

Getter:

SkillDraft? get skillDraft

Boolean helper:

bool get hasSkillDraft

During initialize():

- read the stored locale
- read the stored family profile
- read the stored skill draft
- restore each safely

Only restore a SkillDraft when it belongs to the currently restored teacher profile.

Match:

teacherNickname == profile.nickname

and:

teacherRole == profile.role

If the draft belongs to a different profile, ignore it.

Add:

saveSkillDraft(SkillDraft draft)

This should:

1. write the JSON to AppStorage
2. update the in-memory draft
3. call notifyListeners()

Add:

clearSkillDraft()

This should:

1. clear the stored draft
2. clear the in-memory draft
3. notify listeners

When a profile is replaced or cleared, also clear the associated saved skill draft.

This prevents a draft belonging to an old profile from being shown to a new profile.

==================================================
TEACH SKILL SCREEN
==================================================

Create:

lib/features/skill/presentation/teach_skill_screen.dart

Use a StatefulWidget because the form contains editable state.

Suggested constructor:

TeachSkillScreen({
  required FamilyProfile teacher,
  SkillDraft? initialDraft,
})

The screen must accept:

- the current teacher profile
- an optional existing draft

If initialDraft exists:

- learner nickname field is pre-filled
- learner role is pre-selected
- category is pre-selected
- explanation is pre-filled

This allows the user to continue editing a saved draft.

Use the key:

skill_draft_screen

==================================================
FORM STATE
==================================================

Use TextEditingController for:

- learner nickname
- explanation

Initialize them from initialDraft when available.

Dispose both controllers correctly.

Track:

FamilyRole? selected learner role
SkillCategory? selected category
bool isSaving

Recompute whether Save Draft can be enabled whenever a field changes.

==================================================
SAVE BEHAVIOR
==================================================

When Save Draft is pressed:

1. validate all required values
2. unfocus the keyboard
3. set saving state
4. create SkillDraft using:
   - teacher's saved profile
   - learner nickname
   - learner role
   - category
   - explanation
5. call AppController.saveSkillDraft(...)
6. return to Home after the save completes

If saving fails:

- remain on the screen
- stop the saving state
- show an honest localized error SnackBar

Do not claim data was saved when persistence failed.

==================================================
HOME SCREEN DAY 4 UPDATE
==================================================

Update:

lib/features/home/presentation/home_screen.dart

If there is NO saved skill draft:

continue showing the Day 3 empty family-weave card.

The Teach a Skill button should open:

TeachSkillScreen

with:

initialDraft: null

If a saved draft EXISTS:

replace the empty state with a saved Skill Draft summary card.

The card should show useful draft information such as:

- saved draft title
- learner
- learner role
- selected category
- explanation preview
- local-storage indicator

The explanation preview may be limited to a few lines with ellipsis.

When a draft exists, the Teach button should change conceptually to:

Continue Draft

and should reopen TeachSkillScreen with:

initialDraft: draft

Do not put Expanded or Spacer widgets inside a Column whose height is unbounded by the Home ListView.

Use normal finite spacing such as SizedBox.

The Home screen must remain vertically scrollable.

==================================================
LEARN A SKILL
==================================================

Do not implement Learn a Skill on Day 4.

The existing Learn a Skill button should remain.

It may show the existing localized temporary future-feature SnackBar.

Do not create fake learner flows.

==================================================
LOCALIZATION
==================================================

Update:

lib/l10n/app_en.arb
lib/l10n/app_ar.arb

Add every new user-facing Day 4 string.

Include localization concepts for:

- Teach a Skill screen title
- teacher section
- teacher card body
- learner section
- learner nickname
- learner nickname hint
- learner role
- category section
- each category
- explanation section
- explanation hint/helper
- Save Draft
- Saving Draft
- save error
- saved draft title
- saved draft learner summary
- saved draft category summary
- stored locally message
- Continue Draft

Use ARB placeholders where dynamic values are required.

Do not hard-code English or Arabic user-facing strings inside widgets when they belong in localization.

Run:

flutter gen-l10n

after changing ARB files.

==================================================
RTL
==================================================

The full Teach a Skill screen must work in Arabic RTL.

Verify:

- app bar direction
- back arrow direction
- language action
- section headings
- teacher summary
- learner nickname field
- role cards
- skill category cards
- explanation field
- Save Draft label

Do not blindly mirror icons that are not directional.

==================================================
SKILL CATEGORY UI HELPERS
==================================================

Create:

lib/features/skill/presentation/skill_category_ui.dart

Provide helper functions similar to the existing FamilyRole UI helpers.

The helper should map each SkillCategory to:

- localized label
- Material icon

Do not hard-code category display text directly into the choice-card widget.

==================================================
TESTS — FAMILY PROFILE
==================================================

Keep the FamilyProfile domain tests.

Verify at least:

1. valid JSON round trip
2. damaged JSON returns null
3. unknown roles return null

Do not break these existing tests.

==================================================
TESTS — SKILL DRAFT
==================================================

Create:

test/skill_draft_test.dart

Test at least:

1. SkillDraft survives a JSON round trip.
2. Damaged JSON returns null.
3. Unknown category returns null.
4. Explanation shorter than 20 characters returns null.

Use realistic sample data such as:

Teacher:
Fatima
Grandparent

Learner:
Mariam
Teen

Category:
Heritage

Explanation:
Explain how our family welcomes guests with patience and care.

==================================================
WIDGET TESTS
==================================================

Extend:

test/widget_test.dart

Keep the Day 2 and Day 3 tests.

Add Day 4 tests for at least:

1. Teach a Skill opens from Home.
2. Save Draft starts disabled.
3. Learner nickname alone is insufficient.
4. Learner role is required.
5. Category is required.
6. Explanation must have at least 20 characters.
7. Save Draft becomes enabled only after all required fields are valid.
8. Saving returns to Home.
9. Saved SkillDraft JSON is written to fake storage.
10. Home displays the saved skill draft.
11. Saved draft survives startup restoration.
12. Reopening Teach a Skill restores the previous learner nickname.
13. Reopening restores the previous explanation.
14. Arabic Teach a Skill screen uses RTL.

Use the existing reference surface size:

412 × 892

Use ensureVisible before tapping controls that may be below the current scroll position.

Pump after state-changing interactions before immediately testing or tapping the next state-dependent widget.

Do not hide real rendering overflows by weakening the tests.

Fix the actual widgets.

==================================================
LAYOUT QUALITY
==================================================

Avoid RenderFlex overflows.

For fixed-size role/category cards:

- allow labels to fit safely
- use FittedBox with scaleDown when appropriate
- limit labels to a sensible number of lines

For widgets placed inside a ListView or SingleChildScrollView:

- do not use Spacer
- do not use Expanded vertically unless the parent provides finite height
- prefer mainAxisSize: MainAxisSize.min
- use SizedBox for predictable spacing

Make sure English and Arabic both fit.

==================================================
QUALITY COMMANDS
==================================================

After implementation, run:

flutter gen-l10n

dart format lib test

flutter analyze

flutter test

Do not continue until:

flutter analyze

reports:

No issues found!

and:

flutter test

reports:

All tests passed!

If flutter analyze reports that FakeAppStorage does not implement new AppStorage methods, update FakeAppStorage rather than modifying the tests to avoid the interface.

If flutter test reports RenderFlex overflow, fix the widget causing the overflow.

==================================================
ANDROID EMULATOR VERIFICATION
==================================================

Run the app on the Android emulator.

Verify the complete English flow:

1. Home
2. Tap Teach a Skill
3. Teacher information appears
4. Enter learner nickname
5. Choose learner role
6. Choose category
7. Enter explanation
8. Save Draft
9. Return to Home
10. Saved draft appears
11. Tap Continue Draft
12. Previous values are restored

Verify Arabic:

1. switch to Arabic
2. open Teach a Skill
3. confirm RTL
4. confirm Arabic labels
5. confirm category cards
6. confirm Save Draft
7. confirm Arabic saved-draft Home state

==================================================
PERSISTENCE VERIFICATION
==================================================

Test a real restart.

After saving a draft:

1. close/restart the app
2. confirm the profile still exists
3. confirm the language still exists
4. confirm the skill draft still exists
5. confirm Home displays the saved draft
6. reopen the draft
7. confirm the previous form values are restored

==================================================
DAY 4 SCREENSHOT EVIDENCE
==================================================

Create or use:

docs/testing/screenshots/day-04/

Capture useful emulator evidence, including at least:

- English Teach a Skill screen
- completed English skill form
- English Home with saved draft
- Arabic Teach a Skill screen
- Arabic Home with saved draft

Give me exact Windows PowerShell / ADB commands.

If using ADB directly, do not assume a PowerShell variable such as $adb exists unless it has first been defined.

==================================================
DAY 4 DOCUMENTATION
==================================================

Create/update appropriate Day 4 documentation describing:

Home
→ Teach a Skill
→ Skill Draft
→ local persistence
→ saved draft Home state
→ reopen draft

Document:

- SkillDraft fields
- categories
- explanation limits
- local-only storage
- restoration behavior
- relationship between a saved profile and its saved skill draft
- English/Arabic support
- RTL
- testing results

Keep documentation consistent with the actual implementation.

==================================================
AI USAGE RECORD
==================================================

At the end of Day 4 create:

docs/ai-usage/prompts/2026-08-10-day-04-plan.md

Paste this exact Day 4 user prompt into that file.

Do not include:

- passwords
- tokens
- personal family information
- private keys
- Firebase secrets
- credentials

==================================================
GIT COMMITS
==================================================

Use sensible commit checkpoints rather than one giant commit.

Before each commit:

git status

Review what changed.

Stage only the intended files.

Use clear Day 4 commit messages.

At the end:

git push

Push:

feature/day-04-skill-draft

Prepare it for a pull request into main.

Do not require Shoug to perform Git or VS Code work.

==================================================
DO NOT DO THESE THINGS
==================================================

Do not:

- add Firebase
- add authentication
- add user accounts
- add cloud storage
- add a backend
- collect location
- implement Learn a Skill yet
- build chat
- build social networking
- add family invitations
- add skill matching algorithms
- add unnecessary packages
- duplicate the FamilyRole model
- duplicate existing components unnecessarily
- break Day 2 Welcome
- break Day 3 Profile Setup
- break persistent locale
- break persistent profile
- remove RTL
- hide rendering bugs in tests
- hard-code private data
- store secrets

==================================================
RESPONSE FORMAT
==================================================

Give me Day 4 chronologically from beginning to end.

Number every major step.

For every coding step:

- give the exact file path
- tell me whether to create it or edit it
- tell me whether to replace the entire file or only a section
- provide complete code where appropriate
- give exact PowerShell commands
- immediately include verification after important changes

Keep the instructions beginner-friendly.

Do not skip directly to the final solution.

The final Day 4 result should be a tested bilingual locally persistent Teach a Skill draft flow that works from Home, survives restart, and can be reopened for continued editing.
