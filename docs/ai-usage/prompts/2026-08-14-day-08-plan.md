Assume that all of Day 7 steps are complete and working correctly.

I am continuing the SMAC 2026 Naseej Flutter project for Team Technova.

I am a complete beginner with Flutter, Firebase, AI APIs, Git, Android Studio, and mobile development, so guide me extremely carefully and do not skip steps.

Important working rules:

- Give me one clear step at a time.
- Tell me the exact file path whenever I need to create or edit a file.
- When a Dart file needs substantial changes, give me the complete corrected file rather than isolated snippets.
- Give PowerShell commands one at a time and tell me which folder I must be in before running them.
- Do not assume I know where files, Firebase settings, Android settings, or VS Code options are.
- Stop after each major step so I can show you the result before continuing.
- Do not make unrelated refactors.
- Preserve everything already working from Days 1–7.
- The app must remain bilingual English/Arabic with correct RTL support.
- The app must remain usable if Firebase AI, App Check, or the internet is unavailable.
- The locally saved skill draft must remain usable even if AI generation fails.
- Do not upload context photos to Firebase or Gemini during Day 8.
- Do not put API keys, Firebase credentials, App Check debug tokens, service-account keys, or other secrets in source code, documentation, screenshots, or Git.
- App Check enforcement must remain off unless explicitly required later.
- From Day 3 onward, only I perform VS Code, Flutter, PowerShell, Firebase CLI, Git, and GitHub work. Do not tell Shoug to run coding or Git commands.

Current project:

- App name: Naseej
- Team: Technova
- Repository: axbh25/smac2026-Technova-naseej
- Local project root: C:\dev\smac2026\naseej
- Android package: com.axbh25.naseej
- Flutter Android-first app
- Reference emulator: Pixel 7 Pro
- State management: Provider / ChangeNotifier
- Local profile and draft persistence: SharedPreferences
- Optional speech-to-text already exists
- Optional context photo already exists and stays local
- Firebase Core is configured
- Firebase App Check is configured
- Firebase AI Logic is working
- Day 7 AI Readiness already proves the real AI connection
- Existing tests must continue passing

DAY 8 GOAL

Turn the saved Teach a Skill draft into a useful, structured AI-generated teaching card while keeping Naseej safe, understandable, and usable offline.

Day 8 should introduce the first meaningful AI feature in Naseej.

The feature should take a locally saved skill draft and generate a concise teaching card that helps one family member teach the selected skill to another family member.

The feature must be designed so I can explain exactly:
- what data is sent to AI
- why that data is needed
- what AI returns
- what happens when AI fails
- what remains local
- why the feature strengthens family bonds

Do not make the feature more complicated than necessary.

PART 1 — DAY 8 GIT BRANCH

Start from the latest main branch and create a Day 8 feature branch such as:

feature/day-08-skill-card-generation

Give me the exact Git commands one at a time.

Do not commit until implementation and tests are verified.

PART 2 — DEFINE THE AI FEATURE

The user already creates a SkillDraft containing information such as:
- teacher role
- learner role
- skill category
- explanation of what should be taught

Use this draft to generate a short teaching card.

The generated teaching card should be practical and easy to read on a phone.

A generated card should contain a small number of structured fields, such as:

- title
- short introduction
- 3–5 teaching steps
- one family connection / reflection prompt
- one safety or patience note where appropriate

Keep the output concise.

Do not generate long essays.

Do not make medical, legal, financial, psychological, or other high-risk claims.

PART 3 — PRIVACY BOUNDARY

Be explicit about what Day 8 sends to AI.

Do not send:
- context photo
- context photo path
- raw audio
- App Check token
- Firebase credentials
- device identifiers
- local filesystem paths
- unrelated profile information

If nicknames are not required to generate the teaching card, do not send them.

Prefer role labels such as:
- grandparent
- parent
- adult
- teen
- child

rather than personal names.

Only send the minimum skill information required for the AI feature.

Do not store AI prompts containing private user content in documentation or Git.

Do not include real family explanations or real context photos in test fixtures, screenshots, prompt logs, or documentation.

Use synthetic examples in tests.

PART 4 — SKILL CARD DOMAIN MODEL

Create a simple domain model for the generated teaching card.

Use an appropriate location such as:

lib/features/skill/domain/

The model should represent the structured card in a way that is easy to render and test.

Keep it immutable where practical.

Include only fields needed by the UI.

Do not couple the domain model directly to Firebase classes.

PART 5 — GENERATION SERVICE ABSTRACTION

Create an abstraction such as:

lib/core/ai/skill_card_generation_service.dart

The UI and controller must depend on this abstraction rather than directly depending on Firebase AI.

Define:
- successful result
- expected failure categories
- service contract

Useful failure categories may include:
- offline/timeout
- App Check rejected
- quota exceeded
- service not enabled
- Firebase not configured
- invalid AI response
- unknown failure

Keep the result types simple enough for a beginner to explain.

PART 6 — FIREBASE AI IMPLEMENTATION

Create:

lib/core/ai/firebase_skill_card_generation_service.dart

Use the same Firebase AI Logic setup established on Day 7.

Use the current working Gemini model configured for Naseej.

Do not pass App Check explicitly into FirebaseAI if the current SDK handles App Check automatically.

Firebase App Check should still be activated during app startup.

Use a carefully constrained system instruction.

The system instruction should explain that the model is generating a short family teaching card.

Require concise, safe, structured output.

Do not let the model invent personal facts.

Do not let the model claim that it saw a context photo.

Do not send a context photo during Day 8.

Prefer structured JSON output if the current Firebase AI SDK supports it reliably.

The returned structure should be deterministic enough to parse safely.

Validate the AI response before accepting it.

If required fields are missing or malformed, return an invalid-response failure instead of crashing.

PART 7 — OFFLINE FALLBACK

Naseej must still work during Demo Day if AI or internet access fails.

Create something such as:

lib/core/ai/offline_skill_card_factory.dart

The offline fallback should build a simple useful teaching card locally from the saved draft.

It must not pretend to be AI-generated.

The fallback should use deterministic templates based on:
- skill category
- teacher role
- learner role
- draft explanation

Keep the offline card useful but clearly simpler than the AI-generated version.

Do not require Firebase or internet access for the fallback.

PART 8 — GENERATION CONTROLLER

Create:

lib/core/ai/skill_card_generation_controller.dart

Use ChangeNotifier consistently with the existing app architecture.

Use simple states such as:
- idle
- generating
- ready
- fallback
- unavailable/error

The controller should:
- accept the generation service
- accept the offline factory
- prevent duplicate simultaneous generation calls
- expose the generated card
- expose whether the result came from AI or offline fallback
- expose failure information where useful
- never crash the UI

The controller should make it easy to unit-test generation without making real Firebase calls.

PART 9 — APP STARTUP / PROVIDER

Update:

lib/main.dart

Preserve:
- Firebase initialization
- Firebase App Check activation
- AppController
- SpeechController
- AiReadinessController

Add the SkillCardGenerationController provider.

Reuse a shared FirebaseAI instance if appropriate.

If Firebase initialization fails, provide an unavailable generation service so the rest of the local app can still start.

Do not let Firebase failure prevent Naseej from opening.

PART 10 — SKILL CARD UI

Add the generated teaching-card experience to the existing skill flow.

Do not redesign unrelated screens.

The user should be able to:
1. create or reopen a saved skill draft
2. generate a teaching card
3. see a loading state
4. see the generated card
5. see an offline fallback if AI cannot be reached
6. retry when appropriate

Use clear labels.

The AI feature should never silently run in the background.

The user must deliberately initiate generation.

Use existing Naseej visual tokens:
- AppColors
- AppSpacing
- existing typography
- rounded cards
- accessible buttons
- sufficient contrast
- minimum useful touch sizes

Keep the UI usable on the 412 × 892 reference screen.

PART 11 — AI DISCLOSURE

Clearly tell the user when AI is involved.

Include a short privacy/disclosure message explaining that:
- the teaching draft text may be processed to generate the card
- the Day 6 context photo is not sent to AI during this feature
- the user can continue using the local draft if AI is unavailable

Do not use technical language that ordinary family users would not understand.

PART 12 — ENGLISH AND ARABIC

Add all new UI strings to:

lib/l10n/app_en.arb
lib/l10n/app_ar.arb

Use natural Arabic rather than literal machine-style translation.

Ensure Arabic layouts remain RTL.

Do not manually modify generated localization Dart files.

After ARB changes run:

flutter gen-l10n

from the project root.

PART 13 — FIREBASE AI PROMPT DESIGN

Keep the runtime system prompt in source code concise and specific.

It should instruct Gemini to:
- act as a simple family teaching assistant
- respect the teacher and learner roles
- preserve the user's intended skill
- return practical steps
- use age-appropriate language
- avoid judgmental or patronizing wording
- avoid unsafe recommendations
- avoid inventing facts about the family
- return only the requested structured output

Do not put real user information inside the hard-coded system prompt.

Dynamic user content should be added only at runtime.

PART 14 — OUTPUT VALIDATION

Do not display arbitrary malformed AI output directly.

Validate:
- required title exists
- required steps exist
- steps are not empty
- response is within reasonable size limits

If parsing fails:
- do not crash
- use the offline fallback
- optionally record an invalid-response failure for diagnostics

PART 15 — UNIT TESTS

Create:

test/support/fake_skill_card_generation_service.dart

Use fake services for automated tests.

Never call real Firebase AI from flutter test.

Add controller tests such as:
- successful AI generation returns ready
- offline failure returns fallback
- invalid AI response returns fallback
- duplicate generation calls do not trigger duplicate requests

Add tests for the offline factory.

Use synthetic family/skill data only.

PART 16 — MODEL TESTS

Add focused tests for the generated skill-card domain model.

Test:
- construction
- equality/serialization if used
- required fields
- valid step lists

Do not overengineer the model.

PART 17 — WIDGET TESTS

Update the widget-test harness so it provides:

SkillCardGenerationController

alongside the existing:
- AppController
- SpeechController
- AiReadinessController

Use fake generation services.

Do not let widget tests perform real Firebase requests.

Add focused tests for:
- generate button
- generating state
- successful card display
- offline fallback display
- retry if implemented

Do not remove existing Day 1–7 tests just to make the new tests pass.

If Home or Skill screens become taller, use correct scrolling helpers rather than deleting assertions.

Keep the semantics-heavy accessibility test last.

PART 18 — FIGMA

If Day 8 requires Figma updates, add only the new teaching-card states.

Possible states:
- ready to generate
- generating
- AI-generated card
- offline fallback
- unavailable/error

Maintain English and Arabic equivalents where appropriate.

Do not redesign the full application.

PART 19 — MANUAL REAL AI TEST

After automated tests pass, test the feature with the real Firebase AI service.

Use synthetic non-sensitive draft content.

Verify:
- generation starts only after user action
- Firebase AI responds
- structured card is displayed
- App Check still works
- context photo is not transmitted
- failure does not destroy the saved draft
- offline fallback appears when AI cannot be reached
- retry works

Do not use real private family stories while debugging.

PART 20 — OFFLINE TEST

Test with the emulator disconnected from the internet.

Verify:
- the app still opens
- profile still loads
- saved draft still loads
- Teach a Skill remains usable
- generation fails gracefully
- offline fallback card is produced
- user data remains local

Reconnect the emulator afterward and verify normal AI generation works again.

PART 21 — QUALITY CHECKS

From:

C:\dev\smac2026\naseej

run:

flutter pub get

flutter gen-l10n

dart format lib test

dart format --output=none --set-exit-if-changed lib test

flutter analyze

flutter test

Fix all analyzer and test failures.

Do not accept:
- analyzer issues
- failing tests
- secret values in source code
- real family content in test data

PART 22 — README / DOCUMENTATION

Update README only as needed.

Document:
- Day 8 AI teaching-card feature
- minimal data sent to AI
- context photo remains local
- offline fallback behavior
- Firebase AI Logic architecture
- App Check protection
- testing strategy

Explain the architecture in beginner-friendly language suitable for judge Q&A.

PART 23 — AI USAGE LOG

Update the AI usage documentation.

Record that AI assisted with:
- planning
- code explanation
- debugging
- test planning

Make clear that the student implemented, reviewed, tested, and adapted the changes.

Do not include private runtime user data in the AI usage log.

PART 24 — STORE THIS EXACT DAY 8 PROMPT

Create:

docs/ai-usage/prompts/2026-08-14-day-08-plan.md

Paste this exact current user prompt into that file.

Do not include:
- App Check debug token
- Firebase credentials
- personal family explanations
- real context photos
- service-account key
- API keys

PART 25 — GIT REVIEW

Before staging anything, run:

git status

git diff

Check carefully that:
- no secret is present
- no App Check token is present
- no API key is present
- no personal photo is staged
- no private family explanation is staged
- generated/local temporary files are not accidentally included

PART 26 — COMMIT AND PUSH

Only after:
- flutter analyze passes
- flutter test passes
- real AI generation works
- offline fallback works
- localization works
- privacy boundary is verified

create meaningful Day 8 commit(s).

Use the Day 8 feature branch.

Use descriptive commit messages such as:
- Add AI skill card generation with offline fallback
- Add skill card generation tests and localization
- Document Day 8 AI privacy and generation flow

Push the branch and guide me through opening the pull request.

Do not merge automatically.

FINAL DAY 8 VERIFICATION

At the end verify:

1. Naseej still launches.
2. English still works.
3. Arabic still works.
4. RTL still works.
5. Profile persistence still works.
6. Skill-draft persistence still works.
7. Speech input still works or falls back to typing.
8. Context photo remains optional and local.
9. Firebase initializes.
10. App Check remains active.
11. AI Readiness still works.
12. User can deliberately generate a teaching card.
13. Real Firebase AI generation works.
14. AI output is validated before display.
15. Context photos are not sent to AI.
16. The minimum required draft text is sent.
17. AI failure does not delete or block the local draft.
18. Offline fallback produces a useful card.
19. Automated tests do not call real Firebase.
20. flutter analyze passes.
21. flutter test passes.
22. No secret is committed.
23. No private family data is committed.
24. The Day 8 prompt is stored in docs/ai-usage/prompts/2026-08-14-day-08-plan.md.
25. I can explain the full AI input → model → output → fallback flow during the SMAC judge Q&A.

Do not jump ahead to additional AI features, cloud family accounts, photo analysis, chatbots, recommendations, or social features.

Day 8 is only the first real AI teaching-card generation feature with a reliable local fallback.