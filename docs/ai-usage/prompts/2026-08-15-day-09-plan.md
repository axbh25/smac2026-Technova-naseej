Assume that all of Day 8 steps are complete and working correctly.

I am continuing the SMAC 2026 Naseej Flutter project for Team Technova.

I am a complete beginner with Flutter, Firebase, Git, Android Studio, testing, and mobile development, so guide me extremely carefully and do not skip steps.

Important working rules:

* Give me one clear step at a time.
* Tell me the exact file path whenever I need to create or edit a file.
* When a Dart file needs substantial changes, give me the complete corrected file instead of isolated snippets.
* Give PowerShell commands one at a time and tell me which folder I must be in before running them.
* Do not assume I know where files, Flutter settings, Firebase settings, or VS Code options are.
* Stop after each major step so I can show you the result before continuing.
* Do not make unrelated refactors.
* Preserve everything already working from Days 1–8.
* The app must remain bilingual English/Arabic with correct RTL support.
* The app must remain local-first and usable without internet.
* Do not make Day 9 dependent on Firebase AI.
* Do not send learner progress or teach-back responses to Gemini.
* Do not send the Day 6 context photo to Firebase or Gemini.
* Do not expose API keys, Firebase credentials, passwords, App Check debug tokens, service-account keys, or other secrets.
* Do not place real personal family responses or real teach-back responses in source code, tests, documentation, screenshots intended for GitHub, or Git history.
* Use synthetic examples for development and automated testing.
* From Day 3 onward, only I perform VS Code, Flutter, PowerShell, Firebase CLI, Git, and GitHub work. Do not tell Shoug to run coding or Git commands.

Current project:

* App: Naseej
* Team: Technova
* Repository: axbh25/smac2026-Technova-naseej
* Local project root: C:\dev\smac2026\naseej
* Android package: com.axbh25.naseej
* Flutter Android-first project
* Reference emulator: Pixel 7 Pro
* Reference logical size: approximately 412 × 892
* State management: Provider / ChangeNotifier
* Local persistence: SharedPreferences
* English and Arabic localization already exist
* RTL already works
* Optional speech-to-text already exists
* Optional context photo already exists and remains local
* Firebase Core is configured
* Firebase App Check is configured
* Firebase AI Logic is configured
* AI Readiness already works
* Day 8 can create an AI teaching card with an offline fallback
* Existing tests must continue passing

DAY 9 GOAL

Turn the Day 8 generated Skill Card into an interactive learner-practice experience.

The learner should be able to open the teaching card, practise each step, mark steps as completed, answer a short teach-back question, leave and return later without losing progress, and complete the family lesson.

Day 9 should focus on:

Skill Card
→ learner practice
→ local progress tracking
→ teach-back
→ completion

Day 9 should NOT add another AI call.

The generated Skill Card already contains the lesson content. Day 9 is about helping the learner actually use that content with another family member.

The learner experience must work offline after the Skill Card already exists.

PART 1 — DAY 9 GIT BRANCH

Start from the latest completed Day 8 work.

Create and use:

feature/day-09-learner-practice

Give me the exact Git commands one at a time.

Do not commit until the Day 9 implementation, localization, tests, and manual checks are complete.

PART 2 — DEFINE THE LEARNING FLOW

The learner should be able to open a saved Skill Card and see:

* lesson title
* learner context
* whether the card came from AI or offline fallback
* progress indicator
* safety note
* practice steps
* teach-back question
* reciprocal skill suggestion
* local-save status
* completion action

The basic flow should be:

Open lesson
→ review safety note
→ complete each practice step
→ answer teach-back question
→ complete family lesson
→ see completion confirmation
→ return Home

Keep the experience simple enough for both younger and older family members.

Do not turn it into a complicated learning-management system.

PART 3 — CREATE A LEARNING FEATURE AREA

Keep learner-specific code under an appropriate feature folder such as:

lib/features/learning/

Use subfolders such as:

lib/features/learning/domain/
lib/features/learning/presentation/

Do not move the existing Skill Card domain model just to satisfy this structure.

The generated Skill Card can remain under the skill feature because it is shared between teaching and learning.

PART 4 — LEARNING PROGRESS DOMAIN MODEL

Create:

lib/features/learning/domain/learning_progress.dart

The model should store only what is needed to resume a lesson locally.

It should include concepts such as:

* fingerprint/identifier for the Skill Card being practised
* completed step indexes
* teach-back response
* optional completed timestamp

The model should make it possible to check whether saved progress belongs to the currently opened Skill Card.

Use the Skill Card fingerprint rather than relying only on the title.

Keep the model immutable where practical.

Add safe JSON serialization/deserialization because the progress will be saved locally.

Do not store unnecessary family information inside LearningProgress.

PART 5 — TEACH-BACK VALIDATION

The learner must answer a short teach-back question before completing the lesson.

Use reasonable minimum and maximum text-length limits.

The purpose is not to grade the learner.

The purpose is to encourage the learner to explain what they understood in their own words.

Do not send the response to AI.

Do not evaluate it with Gemini.

Do not assign a score.

The app should only verify that a meaningful response was entered before lesson completion is enabled.

PART 6 — LOCAL PERSISTENCE

Extend the existing AppStorage abstraction so learning progress can be saved locally.

Add methods such as:

* readLearningProgressJson()
* writeLearningProgressJson(...)
* clearLearningProgress()

Implement them in:

SharedPreferencesAppStorage

Update:

FakeAppStorage

for deterministic automated tests.

Keep this progress local to the device.

Do not create Firestore collections.

Do not upload learning progress to Firebase.

Do not require authentication.

PART 7 — APP CONTROLLER

Extend AppController carefully.

It should expose the currently saved LearningProgress.

During initialization, restore saved learning progress if it exists and is valid.

Add clear methods for operations such as:

saveLearningProgress(...)

and, if required:

clearLearningProgress()

Do not break existing:

* profile persistence
* locale persistence
* SkillDraft persistence
* SkillCard persistence

A corrupted or incompatible learning-progress value should not stop Naseej from launching.

PART 8 — MATCH PROGRESS TO THE CORRECT SKILL CARD

Saved progress must only be restored when it belongs to the Skill Card currently being practised.

Use the card's content fingerprint or another deterministic identifier.

If the stored progress belongs to an old or different card:

* do not apply the old completed-step state to the new card
* safely begin with fresh learner progress

Do not accidentally show progress from one family lesson inside another lesson.

PART 9 — LEARN SKILL SCREEN

Create:

lib/features/learning/presentation/learn_skill_screen.dart

Use a StatefulWidget if that makes local editing and autosave behavior easier to understand.

Use the existing SkillCard and SkillDraft as input.

The screen should have a stable key:

learn_skill_screen

The screen should remain usable on the reference 412 × 892 device.

Use a vertical scrollable layout because the lesson can be taller than one screen.

Do not force everything onto one viewport.

PART 10 — LESSON HEADER

At the top of the learner screen, show a clear lesson summary.

Include:

* learner lesson title
* Skill Card title
* AI/offline origin label
* progress count
* progress indicator
* local save status
* short offline/local-storage notice

Use existing Naseej design tokens:

* AppColors
* AppSpacing
* theme typography
* rounded cards
* accessible contrast

Use a stable key such as:

learning_header_card

PART 11 — PRACTICE STEPS

Display the Skill Card steps as interactive learner-practice items.

For each step:

* show the step number
* show the step text
* provide an accessible checkbox/tap target
* visually distinguish completed steps

Use stable keys such as:

learning_step_card_0
learning_step_checkbox_0

and equivalent keys for later steps.

The learner must be able to check and uncheck a step.

Do not require internet access.

PART 12 — PROGRESS INDICATOR

Show clear progress such as:

0 of 3 steps
1 of 3 steps
2 of 3 steps
3 of 3 steps

Use a progress indicator or progress bar.

The progress should update immediately when a learner checks or unchecks a step.

The screen should not claim the full family lesson is complete merely because all checkboxes are checked.

Teach-back must also be completed.

PART 13 — SAFETY NOTE

Show the safety note already contained in the Skill Card.

Use a clearly differentiated information card.

Do not generate a new safety note during Day 9.

Do not make another AI request.

Use the content already present in SkillCard.

PART 14 — TEACH-BACK CARD

Display the Skill Card's existing teach-back question.

Provide an editable text field for the learner's response.

Use a stable key:

teach_back_response_field

Include:

* question heading
* question text
* response label
* response hint
* minimum-length helper text

The response must autosave locally.

Do not send it to AI.

Do not log the response to the console.

Do not include real responses in Git documentation.

PART 15 — RECIPROCAL SKILL SUGGESTION

Show the reciprocal-skill suggestion from the Skill Card.

This should reinforce Naseej's core idea:

Every generation teaches, and every generation learns.

Display the suggestion as a separate information card.

Do not require the reciprocal lesson to be completed during Day 9.

It is only a prompt for future family exchange.

PART 16 — LOCAL AUTOSAVE

Learner progress should save automatically.

For checkbox changes:

* save promptly after the change

For teach-back typing:

* use a short debounce rather than writing SharedPreferences after every keystroke

A debounce around 600 ms is acceptable.

Show understandable save states such as:

* ready to save locally
* changes pending
* saving
* saved on this device
* unable to save

Do not block typing while the debounce timer runs.

Do not lose newer edits if an earlier save is still completing.

PART 17 — COMPLETE LESSON RULES

The primary completion button should remain disabled until:

1. all required practice steps are completed, and
2. the teach-back response passes minimum-length validation.

Use a stable key:

learning_primary_button

Before the requirements are met, the button label may guide the learner with wording such as:

Complete all steps

or:

Write your teach-back

When ready, use wording such as:

Complete family lesson

PART 18 — LESSON COMPLETION

When the learner taps the enabled completion button:

* persist the final progress
* record a completion timestamp locally
* show a clear completion state

Use a stable completion key:

learning_complete_banner

The completion banner should communicate that the family lesson was completed.

Do not award points, rankings, streaks, or competitive scores during Day 9.

This feature is about family learning, not gamification.

PART 19 — RETURN HOME

After completion, the primary button can change to something such as:

Back to Home

The learner must be able to return safely to Home.

Do not delete the completed learning progress merely because the learner returns Home.

Completion should still be restorable if the lesson is reopened.

PART 20 — ENGLISH LOCALIZATION

Update:

lib/l10n/app_en.arb

Add all Day 9 learner-practice strings.

Include strings needed for:

* Learn a Skill title
* learner lesson title
* progress count
* local-save statuses
* offline notice
* safety note
* practice steps
* teach-back question
* teach-back response label
* teach-back hint
* teach-back helper
* reciprocal suggestion
* completion requirements
* complete lesson button
* completed lesson banner
* back to Home
* save error

After changing ARB files, do not manually edit generated localization Dart files.

PART 21 — ARABIC LOCALIZATION

Update:

lib/l10n/app_ar.arb

Translate every new Day 9 key naturally into Arabic.

Do not leave Day 9 English keys untranslated.

Arabic must remain RTL.

After both ARB files are complete, run:

flutter gen-l10n

from:

C:\dev\smac2026\naseej

If Flutter reports untranslated Arabic messages after replacing only the English ARB file, that is expected temporarily until the Arabic ARB file is updated.

After completing both ARB files, the untranslated-message warning should disappear.

PART 22 — HOME / NAVIGATION ENTRY

Connect the existing Learn a Skill action to the new learner screen.

The learner screen should use the saved Skill Card.

Do not generate a new card every time Learn a Skill is opened.

If no Skill Card is available, preserve the existing safe/empty behavior rather than crashing.

Do not disturb the Teach a Skill flow.

PART 23 — OFFLINE-FIRST REQUIREMENT

Day 9 must work without internet once a Skill Card already exists locally.

Test this conceptually:

Saved Skill Card
→ disconnect internet
→ open Learn a Skill
→ complete practice steps
→ type teach-back
→ leave/reopen
→ progress remains

Firebase AI must not be required for this learner-practice flow.

PART 24 — PRIVACY REQUIREMENT

Day 9 learner data stays local.

Do not transmit:

* completed step indexes
* teach-back response
* completion timestamp
* personal family response
* context photo
* context photo path

to Gemini or another remote AI service.

The teach-back response is private learner reflection data.

Do not put it in debug logging.

PART 25 — LEARNING PROGRESS TESTS

Add unit tests for LearningProgress.

Test important behavior such as:

* valid construction
* JSON round trip
* completed-step restoration
* teach-back restoration
* completion timestamp
* card fingerprint matching
* malformed or invalid persisted values where appropriate

Use synthetic values only.

PART 26 — STORAGE / CONTROLLER TESTS

Update relevant storage and AppController tests.

Verify:

* progress can be saved
* progress can be restored
* progress belongs to the correct Skill Card
* existing persisted profile/draft/card behavior remains intact

Do not call Firebase from these tests.

PART 27 — LEARN SKILL WIDGET TESTS

Create:

test/learn_skill_screen_test.dart

Add focused widget tests.

At minimum test:

1. learner completes all steps and teach-back
2. partially completed progress restores correctly
3. Arabic learner screen uses RTL

Use synthetic data such as a generic family skill.

Never put a real teach-back response in the tests.

Use the 412 × 892 reference surface.

Because the learner screen is a ListView, remember that widgets farther down the page may not be built yet.

Use:

tester.scrollUntilVisible(...)

when a test needs a widget that may be outside the currently built viewport.

Do not call tester.widget(...) or ensureVisible(...) on a finder that currently matches zero widgets.

After lesson completion, remember that the completion banner appears near the top of the ListView, so a test that is currently scrolled to the bottom may need to scroll upward before asserting the banner.

PART 28 — EXISTING TEST SUITE

Do not delete or weaken existing Days 1–8 tests just to make Day 9 pass.

Preserve tests for:

* Welcome
* localization
* profile setup/persistence
* Teach a Skill
* speech
* context photo
* AI Readiness
* Skill Card generation
* offline fallback
* Skill Card model

If Day 9 makes an existing screen taller, fix test scrolling correctly.

Keep the semantics-heavy accessibility test last if that remains part of the main widget suite.

PART 29 — FIGMA

If the Day 9 guide requires Figma updates, add only the learner-practice states needed for handoff.

Possible states:

* lesson starting / 0 steps complete
* partial progress
* all steps complete but teach-back required
* ready to complete
* completed lesson

Include English and Arabic/RTL equivalents where practical.

Do not redesign unrelated Days 1–8 screens.

PART 30 — MANUAL TEST

Run Naseej on the Android emulator.

Use synthetic lesson content.

Verify manually:

* Learn a Skill opens
* title appears
* safety note appears
* all practice steps appear
* checkbox state updates
* progress count updates
* teach-back can be typed
* progress autosaves
* leaving/reopening restores progress
* completion button stays disabled until requirements are met
* completed lesson shows the completion banner
* Back to Home works
* Arabic uses RTL

Do not use real private family responses while testing.

PART 31 — CLEAR LOCAL TEST DATA WHEN NEEDED

If a clean first-run state is needed during manual testing, use Android's package manager to clear only Naseej's emulator data.

Package:

com.axbh25.naseej

Be aware that clearing app data removes locally stored test:

* profile
* SkillDraft
* SkillCard
* learning progress
* preferences

Do not do this accidentally when trying to test persistence.

PART 32 — QUALITY CHECKS

From:

C:\dev\smac2026\naseej

run the appropriate checks, including:

flutter pub get

flutter gen-l10n

dart format lib test

dart format --output=none --set-exit-if-changed lib test

flutter analyze

flutter test

Fix all actual analyzer and test failures.

Do not accept:

* analyzer issues
* failing tests
* untranslated Day 9 strings
* overflow errors
* real private family data in test fixtures
* secrets in tracked files

PART 33 — README / DOCUMENTATION

Update README only as needed.

Document Day 9 at a high level:

* learner-practice flow
* local progress persistence
* teach-back
* offline behavior
* privacy boundary
* no additional AI call

Make the architecture understandable enough that I can explain it during judge Q&A.

PART 34 — AI USAGE LOG

Update the project AI-assistance documentation.

Record that AI was used to assist with:

* planning
* code explanation
* debugging
* localization guidance
* test planning

Make clear that the student:

* implemented the changes
* ran the app
* verified the behavior
* fixed issues
* ran analyzer/tests

Do not include private runtime content in the AI usage log.

PART 35 — PRIVACY REVIEW

Before committing, verify that tracked files do not contain:

* App Check debug token
* API keys
* passwords
* Firebase credentials
* service-account keys
* private context photos
* personal family responses
* real teach-back responses

Synthetic test data is allowed.

PART 36 — GIT REVIEW

Before staging, run:

git status

git diff

Review every Day 9 change.

Make sure only intended Day 9 files are included.

Then stage carefully and inspect:

git diff --cached

Do not commit secrets or personal data.

PART 37 — STORE THIS EXACT DAY 9 AI-ASSISTANCE PROMPT

Create:

docs/ai-usage/prompts/2026-08-15-day-09-plan.md

Paste this exact current user prompt into that file.

Do not include:

* App Check debug token
* API keys
* passwords
* private context photos
* personal family responses
* real teach-back responses
* Firebase login credentials
* service-account keys

The prompt file should document the AI assistance requested for Day 9 without exposing runtime secrets or private family information.

PART 38 — COMMIT AND PUSH

Only after:

* learner-practice flow works
* local progress restores
* teach-back works
* completion works
* English and Arabic work
* RTL works
* flutter analyze passes
* flutter test passes
* privacy review passes

create meaningful Day 9 commit(s).

Use:

feature/day-09-learner-practice

Use descriptive commit messages such as:

Add local learner practice and teach-back flow

Add persistent learning progress and completion state

Add bilingual learner practice tests and documentation

Push the branch and guide me through opening the Day 9 pull request.

Do not merge automatically.

FINAL DAY 9 VERIFICATION

At the end verify:

1. Naseej still launches on Android.
2. Welcome still works.
3. English still works.
4. Arabic still works.
5. RTL still works.
6. Profile persistence still works.
7. SkillDraft persistence still works.
8. Optional speech input still works or falls back to typing.
9. Optional context photo still works and remains local.
10. Firebase initialization still works.
11. App Check still works.
12. AI Readiness still works.
13. Day 8 Skill Card generation still works.
14. Day 8 offline Skill Card fallback still works.
15. A saved Skill Card can be opened in Learn a Skill.
16. Practice steps can be completed.
17. Progress count updates correctly.
18. Learning progress saves locally.
19. Partially completed progress restores after reopening.
20. Teach-back is required before completion.
21. Teach-back response stays local and is not sent to AI.
22. Lesson completion persists locally.
23. Completion banner appears.
24. Back to Home works.
25. Learner practice works offline with an already saved Skill Card.
26. No additional Day 9 Gemini call was added.
27. No context photo is sent to AI.
28. flutter gen-l10n completes without untranslated Day 9 messages.
29. flutter analyze passes.
30. flutter test passes.
31. No App Check debug token is committed.
32. No API key, password, or Firebase credential is committed.
33. No private context photo is committed.
34. No personal family response is committed.
35. No real teach-back response is committed.
36. The Day 9 prompt is stored at docs/ai-usage/prompts/2026-08-15-day-09-plan.md.
37. All Day 9 work is on feature/day-09-learner-practice.
38. I can explain that Day 9 converts an AI/offline teaching card into a private, local, offline-capable family learning experience.

Do not jump ahead to Day 10 features.

Day 9 is only the learner-practice, progress, teach-back, and local completion experience built on top of the Day 8 Skill Card.
