Assume that all of Day 1 steps are complete and verified successfully for the Naseej SMAC 2026 project.

The app name will remain **Naseej**.

Assume the following are already complete from Day 1:

- Flutter is installed and working.
- Flutter is added to PATH.
- VS Code is installed with the Flutter and Dart extensions.
- Android Studio is installed.
- The Android SDK is installed.
- A Pixel 7 Pro Android emulator is created and working.
- The Flutter project has already been created as an Android-only project.
- The project is located at:
  C:\dev\smac2026\naseej
- The GitHub repository already exists.
- The local project is connected to the GitHub repository.
- The `main` branch is synchronized with GitHub.
- The default Flutter application has already been tested on the emulator.
- `flutter analyze` works.
- `flutter test` works.
- The basic project documentation and AI-usage documentation from Day 1 already exist.
- The Figma file for Naseej already exists.
- The app will support both **English and Arabic**.
- The primary emulator/reference screen size is **412 × 892 logical pixels**.
- The project is being developed by **Team Technova**.
- Abdullah Haider is working mainly on Flutter development, architecture, testing, and documentation.
- Shoug Almaashari is working mainly on Figma, documentation, and testing.
- Both of us are beginners, so assume we need very detailed instructions.
- We will use some AI assistance for coding and design, but we must understand, review, test, and document everything that is generated.
- We will use Figma as the design source of truth before implementing screens in Flutter.

@GitHub

First, read through the current Naseej repository and understand the exact current state of the project before planning Day 2.

Do not assume files exist unless you verify them.

Please:

1. Summarize what was completed on Day 1.
2. Identify the current Flutter project structure.
3. Identify the existing documentation files.
4. Identify the current testing setup.
5. Identify anything that needs to be corrected before Day 2 development starts.
6. Identify anything in the repository that could create problems later.
7. Do not make changes to the repository while doing this initial review.

Then tell me **everything and exactly what to do for Day 2**.

I want the instructions to be extremely detailed and beginner-friendly.

Treat this as if Shoug and I are building our first real Flutter application.

Do not skip steps because they seem obvious.

For every step:

- Give the step a number and clear title.
- Tell me exactly what we are doing.
- Explain why we are doing it.
- Tell me which program to use.
- Tell me where to click when working in Figma, VS Code, Android Studio, GitHub, or another graphical tool.
- Give the exact terminal command whenever a command is required.
- Tell me exactly which directory the command must be executed from.
- Tell me exactly which file or folder to create.
- Tell me the exact path of every file.
- If code is required, give me the full code that should be placed in that file.
- Explain what the important parts of the code do in simple language.
- Tell me what I should expect to see after completing the step.
- Give me a simple verification step before moving forward.
- If something can damage, delete, overwrite, or replace existing work, warn me first.
- Do not use destructive Git commands unless absolutely necessary.
- Do not use `git push --force`.
- Never include passwords, tokens, API keys, secrets, or private credentials in documentation or Git.

# Day 2 Main Goal

The goal for Day 2 should be to move Naseej from the default Flutter starter application to the first real, designed, bilingual Naseej screen.

By the end of Day 2 I want:

- a proper Figma foundation for the first screen;
- reusable Figma components;
- an English Welcome screen;
- an Arabic Welcome screen;
- proper RTL behavior for Arabic;
- a clean Flutter source structure;
- shared Flutter design tokens/theme;
- Flutter localization using ARB files;
- immediate English/Arabic switching;
- the Welcome screen implemented in Flutter;
- the Flutter implementation to closely match the Figma design;
- automated tests;
- accessibility checks;
- emulator verification;
- screenshots as testing evidence;
- clear Day 2 documentation;
- clean Git commits;
- a feature branch and Pull Request workflow;
- the completed work merged safely back into `main`.

# Figma Work

We are designing in Figma before implementing the screen in Flutter.

Guide us through the Figma work in very small steps.

The Figma file should remain organized into pages such as:

- `00_Cover`
- `01_Foundations`
- `02_Components`
- `03_Screens_EN`
- `04_Screens_AR`
- `05_Prototype`
- `06_Dev_Handoff`
- `07_QA_Overlays`

If these already exist, use them rather than recreating them.

## Foundations

Help us establish the design foundations needed for the Welcome screen, including:

- colors;
- spacing;
- typography;
- English typography;
- Arabic typography;
- background;
- surfaces;
- borders;
- primary color;
- accent color;
- disabled colors;
- pressed states;
- reusable Figma variables/styles.

Do not create unnecessary design-system complexity.

Keep it realistic for a small hackathon/MVP project.

## Components

Create reusable Figma components for the Welcome screen rather than drawing the same UI repeatedly.

The Day 2 Welcome screen should use reusable components such as:

1. Naseej Brand Mark
2. Language Selector
3. Privacy Notice Card
4. Primary Button

Where appropriate, use Figma variants for states such as:

- English / Arabic
- Default / Pressed / Disabled

Use Auto Layout where it makes sense.

Keep component dimensions, spacing, colors, typography, and variant names explicit so they can later be reproduced accurately in Flutter.

## English Welcome Screen

Create the English screen on `03_Screens_EN`.

Use a reference frame of:

- Width: 412
- Height: 892

The screen should contain:

- English-selected language control;
- Naseej branding;
- Arabic branding as part of the identity;
- English tagline;
- English privacy notice;
- primary Continue button.

The design should be clean, modern, minimal, and appropriate for Naseej.

## Arabic Welcome Screen

Create the Arabic version on `04_Screens_AR`.

Use the same:

- 412 × 892 dimensions;
- spacing system;
- design language;
- component sizes;
- visual hierarchy.

But intentionally adapt the screen for Arabic.

Do NOT simply horizontally mirror the entire English screen.

Use proper RTL behavior where required.

For example:

- Arabic language state selected;
- selector positioned appropriately for the Arabic composition;
- Arabic copy;
- Arabic privacy-card variant;
- icon/text order appropriate for RTL;
- Arabic button text;
- RTL text behavior.

The central brand area can remain visually centered where appropriate.

# Figma → Flutter Accuracy

The Flutter implementation should match the approved Figma design as closely as practical.

When translating Figma to Flutter, explicitly identify:

- screen size;
- width;
- height;
- margins;
- padding;
- spacing;
- colors;
- font sizes;
- font weights;
- line heights;
- border radii;
- component heights;
- component widths;
- alignment;
- RTL behavior.

Do not randomly invent measurements if Figma already defines them.

Prefer reusable Flutter widgets over duplicated UI.

However, keep the Flutter architecture understandable for beginners.

# Flutter Project Structure

Guide us in creating a clean but simple Flutter source structure.

A structure similar to this is appropriate:

lib/
├── main.dart
├── app.dart
├── core/
│   └── theme/
│       ├── app_colors.dart
│       ├── app_spacing.dart
│       └── app_theme.dart
├── features/
│   └── welcome/
│       └── presentation/
│           ├── welcome_screen.dart
│           └── widgets/
│               └── language_segmented_control.dart
└── l10n/
    ├── app_en.arb
    ├── app_ar.arb
    └── generated localization files

Do not introduce Clean Architecture, BLoC, Riverpod, Provider, dependency injection, repositories, use cases, or other advanced architecture unless it is genuinely necessary at this stage.

Keep Day 2 simple.

# Localization

Set up proper Flutter localization for:

- English
- Arabic

Use Flutter's normal localization workflow and ARB files.

Create the necessary localization configuration.

Use English as the initial/default language.

The user should be able to switch between English and Arabic immediately from the Welcome screen.

Arabic must use RTL layout behavior correctly.

Do not hard-code every translated string directly into the screen widget if it belongs in localization files.

# Welcome Screen Functionality

The Welcome screen should:

- launch in English by default;
- display the Naseej brand;
- display the correct tagline;
- display the privacy message;
- display the primary button;
- allow switching to Arabic;
- update the visible localized text immediately;
- use RTL direction in Arabic;
- switch back to English correctly.

The Continue button does not need to navigate to a future feature if that feature has not been built yet.

If navigation is not implemented yet, use honest temporary feedback rather than pretending the next feature already exists.

# Code Quality

Before accepting code:

- format it;
- run static analysis;
- inspect warnings/errors;
- make sure imports are clean;
- avoid unnecessary dependencies;
- avoid dead code;
- use meaningful names;
- keep widgets reasonably small.

Tell me when to run:

```powershell
dart format .
flutter gen-l10n
flutter analyze
flutter test
