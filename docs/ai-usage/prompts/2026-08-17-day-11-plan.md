Assume that all of Day 10 steps are complete and working correctly.

I am continuing the SMAC 2026 Naseej Flutter project for Team Technova.

I am a beginner with Flutter, Android, Firebase, Git, testing, state management, and mobile development, so guide me carefully and do not skip important implementation steps.

IMPORTANT COMPETITION RULE

AI is being used only as a documented supporting tool for planning, explanation, debugging, localization guidance, and test guidance.

Do NOT generate the entire application for me as an autonomous replacement for student development.

I must understand the architecture, code, privacy decisions, data flow, and testing well enough to explain the project myself during judging and Q&A.

When suggesting code changes:
- explain what the change does
- keep the architecture understandable
- avoid unnecessary complexity
- preserve the work I have already implemented
- help me verify the result myself

Important working rules:

- Give me clear, concrete steps.
- Tell me the exact file path whenever a file must be created or edited.
- For substantial Dart changes, provide the complete corrected file rather than scattered snippets when practical.
- Give PowerShell commands clearly and assume the project root is:
  C:\dev\smac2026\naseej
- Do not make unrelated refactors.
- Preserve everything working from Days 1–10.
- Keep the app bilingual in English and Arabic.
- Preserve correct Arabic RTL behavior.
- Keep learner and family data local-first.
- Do not add another Gemini request for Day 11.
- Do not upload Family Thread responses to Firebase.
- Do not upload teach-back responses.
- Do not upload private context photos.
- Do not introduce authentication, Firestore, or cloud storage just for Day 11.
- Do not expose API keys, passwords, Firebase credentials, App Check debug tokens, service-account keys, or other secrets.
- Do not place real family responses, real teach-back answers, private family photographs, or personal family data in source code, tests, documentation, screenshots intended for GitHub, or Git history.
- Use synthetic examples for tests and documentation.
- From Day 3 onward, only I perform VS Code, Flutter, PowerShell, Git, Firebase CLI, and GitHub work. Do not tell Shoug to run coding or Git commands.

CURRENT PROJECT

App:
Naseej

Team:
Technova

Repository:
axbh25/smac2026-Technova-naseej

Local root:
C:\dev\smac2026\naseej

Android package:
com.axbh25.naseej

Current architecture includes:

- Flutter Android-first application
- Provider / ChangeNotifier
- SharedPreferences local persistence
- English and Arabic localization
- RTL support
- local family profile
- Teach a Skill draft
- optional speech-to-text input
- optional local context photo
- Firebase Core
- Firebase App Check
- Firebase AI Logic
- AI readiness check
- AI-generated or offline fallback Skill Card
- three-step learner-practice flow
- teach-back response
- persistent LearningProgress
- optional device text-to-speech playback
- Listen / Stop / Replay controls
- language-aware TTS
- safe TTS unavailable fallback

DAY 11 GOAL

Add the Family Thread.

The Family Thread should complete Naseej's core intergenerational exchange:

One family member teaches
→ another family member learns
→ the learner explains what they understood
→ the learner teaches something back
→ the exchange becomes a completed Family Thread

This feature should reinforce Naseej's central idea:

Every generation teaches.
Every generation learns.

Day 11 is NOT another AI-generation feature.

The reciprocal suggestion already exists inside the saved Skill Card.

Day 11 should turn that suggestion into a simple, private, locally saved reciprocal family exchange.

DAY 11 FLOW

The intended experience is:

Teacher shares skill
→ learner completes three steps
→ learner completes teach-back
→ original family lesson is completed
→ Family Thread appears
→ learner chooses or writes something to teach back
→ Family Thread is completed
→ completed exchange is visible later

PART 1 — DAY 11 BRANCH

Use:

feature/day-11-family-thread

Start from the completed Day 10 work.

Do not mix unrelated future features into this branch.

Before final commit, all analyzer and tests must pass.

PART 2 — EXTEND LEARNING PROGRESS

Update:

lib/features/learning/domain/learning_progress.dart

Extend LearningProgress so it can represent the reciprocal exchange.

Keep the existing fields:

- skillCardFingerprint
- completedStepIndexes
- teachBackResponse
- completedAtIso8601

Add fields for the Family Thread:

- returnSkillResponse
- exchangeCompletedAtIso8601

The return skill should default safely to an empty string so existing Day 9/10 data remains compatible where practical.

The Family Thread fields must remain part of the same local learner-progress record because they belong to the same Skill Card exchange.

PART 3 — RETURN-SKILL VALIDATION

Add reasonable validation limits.

A short reciprocal skill response should be accepted.

Use constants such as:

minimumReturnSkillLength
maximumReturnSkillLength

The purpose is simply to prevent an empty response.

Do not perform AI grading.

Do not score the return skill.

Do not judge the quality of the family's response.

PART 4 — FAMILY THREAD COMPLETION STATE

Add a derived property such as:

isExchangeCompleted

It should only become true when:

- the original lesson is completed
- the return-skill response is valid
- exchangeCompletedAtIso8601 exists

A Family Thread cannot be complete if the original lesson itself has not been completed.

PART 5 — JSON PERSISTENCE

Update LearningProgress JSON serialization.

Store:

- returnSkillResponse
- exchangeCompletedAtIso8601

The parser must remain defensive.

Reject malformed values safely rather than crashing the app.

Older saved learning-progress JSON that does not contain the new Day 11 fields should continue loading with:

returnSkillResponse = ''

and:

exchangeCompletedAtIso8601 = null

where practical.

PART 6 — INVALID COMPLETION STATE

If a previously completed Family Thread is edited afterward:

- clear exchangeCompletedAtIso8601
- mark the Family Thread incomplete again
- autosave the updated return skill

This prevents the UI from claiming the exchange is still complete when its final response changed.

Similarly, if the original lesson is invalidated, the Family Thread completion state must also be invalidated.

PART 7 — LEARN SKILL SCREEN

Continue using:

lib/features/learning/presentation/learn_skill_screen.dart

Do not create a separate complex navigation flow unless clearly necessary.

The Family Thread should appear in the existing learner experience after the original lesson is completed.

Preserve:

- lesson header
- saved-progress status
- TTS support
- safety note
- three steps
- teach-back
- original lesson completion
- existing local autosave

PART 8 — RETURN-SKILL TEXT CONTROLLER

Add a TextEditingController for the return skill.

Example:

_returnSkillController

Restore the value from existing LearningProgress when the screen opens.

Dispose it correctly.

Add a change listener.

Use the same general local autosave architecture as the teach-back response.

Do not create a second unrelated persistence system.

PART 9 — FAMILY THREAD VISIBILITY

The Family Thread should NOT appear before the original learner lesson is completed.

Only show it when:

_isLessonCompleted

is true.

This gives the experience a clear sequence:

learn first
→ then teach back

PART 10 — FAMILY THREAD CARD

Create a Family Thread card inside LearnSkillScreen.

Use the stable key:

family_thread_card

The card should visually communicate connection between the two family members.

Include information such as:

- teacher nickname
- learner nickname
- skill that was taught
- what the learner said they learned
- Naseej's reciprocal skill suggestion
- editable return skill
- privacy/local-storage notice
- action to complete the Family Thread

Use a simple intergenerational connection visual.

For example:

person
↔
person

The design should fit Naseej's established visual style.

PART 11 — THREAD SUMMARY

Inside the Family Thread card, summarize the exchange.

Show concepts such as:

Taught:
the saved Skill Card title

Learned:
the learner's existing teach-back response

Suggested return skill:
the existing reciprocalSkillSuggestion from the Skill Card

Do not create another AI request.

Do not regenerate the reciprocal suggestion.

Use the suggestion already stored in the Skill Card.

PART 12 — USE NASEEJ SUGGESTION

Add a convenience button with stable key:

use_return_suggestion_button

The button should copy:

widget.card.reciprocalSkillSuggestion

into the return-skill response field.

The learner must still be able to edit the text afterward.

Using the suggestion should NOT automatically complete the Family Thread.

PART 13 — RETURN-SKILL FIELD

Add an editable field with stable key:

return_skill_response_field

The learner can describe what they will teach back.

Use wording appropriate to the learner and family exchange.

Limit the input using:

LearningProgress.maximumReturnSkillLength

The response remains local.

Do not send it to Gemini.

Do not send it to Firebase AI Logic.

Do not log the response.

PART 14 — AUTOSAVE

Return-skill changes should autosave locally.

Reuse the existing debounce approach.

Approximately:

600 ms

is acceptable.

Saving the return skill must preserve:

- completed step indexes
- teach-back response
- original lesson completion timestamp
- current return skill
- Family Thread completion state

Do not overwrite unrelated progress fields.

PART 15 — COMPLETE FAMILY THREAD BUTTON

Add a button with stable key:

complete_family_thread_button

The button must remain disabled until:

- the original lesson is complete
- returnSkillResponse is valid
- no save is currently blocking the action

Use localized text through:

completeFamilyThreadLabel

Do not hard-code English directly inside the widget.

PART 16 — COMPLETE FAMILY THREAD ACTION

When the learner completes the Family Thread:

- dismiss the keyboard if needed
- stop TTS if playing
- persist the final return skill
- create exchangeCompletedAtIso8601
- save the updated LearningProgress locally

The timestamp should be stored in UTC ISO-8601 format like the existing lesson-completion timestamp.

PART 17 — COMPLETION BANNER

After successful completion, display a Family Thread completion banner.

Use stable key:

family_thread_complete_banner

The banner should confirm that the two-way family exchange has been completed.

It can include:

- the Family Thread visual
- completion title
- a short explanation
- local/privacy notice

Do not add points, rankings, public sharing, or competitive gamification.

PART 18 — COMPLETION BUTTON AFTER COMPLETION

Once the Family Thread is completed:

- do not let the user repeatedly complete the same unchanged exchange
- show a clear completed state

If the return-skill text is later edited, completion should become invalid and the user should be able to complete the revised Family Thread again.

PART 19 — EXISTING BACK TO HOME BEHAVIOR

Preserve the main lesson button.

Once the original lesson is completed, it can still show:

Back to Home

Do not make Family Thread completion mandatory before returning Home.

The learner may finish the reciprocal exchange later.

PART 20 — SAVE BEFORE LEAVING

If there are unsaved return-skill edits when Back to Home is pressed:

- persist them safely before popping the screen where practical

Also stop active text-to-speech before leaving.

PART 21 — HOME SCREEN

Update:

lib/features/home/presentation/home_screen.dart

Recognize the new Family Thread state.

If:

learningProgress.isExchangeCompleted == true

show a Family Thread summary on Home.

Use stable key:

family_thread_home_summary

The summary should communicate that the two-way family exchange is complete.

PART 22 — HOME FAMILY THREAD SUMMARY

The Home summary can include:

- Family Thread title
- teacher and learner connection
- reciprocal skill that was returned
- local/private storage notice

Keep it concise.

The detailed exchange remains available through Learn a Skill.

PART 23 — HOME ACTION LABEL

Update the Learn action appropriately.

Possible states:

No progress:
Start Learning

Partial progress:
Continue Learning

Completed lesson:
Review Completed Lesson

Completed Family Thread:
Review Family Thread

Use localization rather than hard-coded strings.

PART 24 — PRIVACY BOUNDARY

Day 11 Family Thread data stays local.

Do not transmit:

- returnSkillResponse
- teachBackResponse
- completion timestamps
- family nicknames
- private photographs
- context photo path

to Gemini as part of Day 11.

Do not create Firestore documents.

Do not upload to Cloud Storage.

Do not require user accounts.

PART 25 — NO NEW DAY 11 AI CALL

Do NOT add:

- Family Thread generation call
- AI scoring
- AI evaluation
- AI sentiment analysis
- AI rewriting of the return skill
- AI recommendations based on private teach-back content

The existing Skill Card reciprocal suggestion is sufficient.

Day 11 converts existing content into a family interaction.

PART 26 — ENGLISH LOCALIZATION

Update:

lib/l10n/app_en.arb

Add all Family Thread strings.

Include concepts such as:

- Family Thread
- Family Thread explanation
- connection label
- taught skill
- what learner learned
- reciprocal suggestion
- return-skill label
- return-skill hint
- helper text
- use Naseej suggestion
- complete Family Thread
- saving
- completed
- completion banner
- Home summary
- review Family Thread
- local privacy notice

Do not manually edit generated localization Dart files.

PART 27 — ARABIC LOCALIZATION

Update:

lib/l10n/app_ar.arb

Translate every new Day 11 string naturally into Arabic.

Preserve RTL behavior.

Use clear family-friendly Arabic.

After updating both files run:

flutter gen-l10n

There should be no untranslated Day 11 messages.

PART 28 — LEARNING PROGRESS TESTS

Extend the LearningProgress tests.

Test:

- returnSkillResponse JSON round trip
- exchangeCompletedAtIso8601 JSON round trip
- old JSON without Day 11 fields remains valid
- invalid return skill does not complete exchange
- valid completed exchange reports isExchangeCompleted
- malformed exchange timestamp is rejected
- exchange completion without completed original lesson is rejected
- equality/hash behavior includes Day 11 fields

Use synthetic data only.

PART 29 — FAMILY THREAD FLOW TEST

Create or update:

test/family_thread_flow_test.dart

Test at minimum:

1. completed lesson reveals the Family Thread card
2. using reciprocal suggestion fills and saves the return skill
3. suggestion can complete and persist the Family Thread
4. editing a completed return skill invalidates Family Thread completion
5. Arabic Family Thread screen uses RTL

Use the existing fake AppStorage.

Use the fake TTS engine.

Do not call real Android TTS.

Do not call Firebase.

PART 30 — LISTVIEW TESTING

LearnSkillScreen is a long ListView.

Flutter widget tests only build some off-screen children.

Therefore, before interacting with Family Thread widgets, use:

tester.scrollUntilVisible(...)

with stable keys.

Do not assume:

family_thread_card
use_return_suggestion_button
return_skill_response_field
complete_family_thread_button

are immediately built.

After Family Thread completion, remember that:

family_thread_complete_banner

is inserted ABOVE the Family Thread card.

If the test is currently scrolled near the bottom, scroll upward before asserting the completion banner.

Prefer localization-driven text assertions instead of duplicating literal English/Arabic strings when possible.

PART 31 — QUALITY CHECKS

From:

C:\dev\smac2026\naseej

run:

flutter pub get

flutter gen-l10n

dart format lib test

dart format --output=none --set-exit-if-changed lib test

flutter analyze

flutter test

Fix every genuine analyzer issue and failing test.

Do not weaken existing tests merely to make the suite green.

Preserve Days 1–10 behavior.

PART 32 — STORE THE EXACT DAY 11 AI PROMPT

Create:

docs/ai-usage/prompts/<ACTUAL-DATE>-day-11-plan.md

Paste the exact user request that began Day 11 and the continuation request into this file.

Also retain the important Day 11 planning notes needed to document how AI was used as a supporting tool.

Do NOT include:

- API keys
- App Check debug tokens
- passwords
- Firebase credentials
- service-account keys
- private family photographs
- real family teach-back responses
- real return-skill responses
- real personal family data

The competition permits AI only as a documented supporting tool.

It does NOT permit AI to generate the full application as a replacement for student development.

The team must understand the application, architecture, privacy decisions, implementation, and tests well enough to answer judge questions during Q&A.

RETAINED DAY 11 PLANNING NOTES

- Day 11 adds the Family Thread reciprocal exchange.
- Family Thread appears only after the original lesson is completed.
- It uses the reciprocalSkillSuggestion already stored in SkillCard.
- It does not make another Gemini request.
- LearningProgress is extended with returnSkillResponse.
- LearningProgress is extended with exchangeCompletedAtIso8601.
- isExchangeCompleted requires the original lesson, a valid return skill, and an exchange completion timestamp.
- Editing a completed return skill invalidates Family Thread completion.
- Return-skill text autosaves locally.
- A Use Naseej Suggestion button fills the editable return-skill field.
- The Family Thread completion state remains local.
- Family Thread completion is not mandatory before returning Home.
- Home shows a Family Thread summary once the reciprocal exchange is completed.
- English and Arabic localization must both be implemented.
- Arabic RTL must remain correct.
- Widget tests must account for the lazy ListView.
- Stable keys should be used for Family Thread tests.
- AI remains a documented planning/debugging/testing aid only.
- The student must understand and be able to explain all implemented changes.

PART 33 — DOCUMENTATION

Update project documentation only as needed.

Describe Day 11 simply:

Naseej turns a completed one-way lesson into a two-way family exchange.

The original learner can teach something back, creating a Family Thread.

Explain that:

- return-skill data stays local
- no extra Gemini request occurs
- no cloud storage is required
- completion is persisted locally

Document AI assistance transparently.

Do not suggest that AI independently built the application.

PART 34 — PRIVACY AND AI-ASSISTANCE REVIEW

Before staging, inspect tracked changes.

Confirm there are no:

- API keys
- App Check debug tokens
- passwords
- service-account credentials
- private family photographs
- real family responses
- real teach-back responses
- real return-skill responses
- sensitive personal user data

Also confirm the AI-use documentation accurately describes AI as a supporting development tool.

PART 35 — GIT REVIEW

Before committing run:

git status

git diff

Review all Day 11 changes.

Then stage intentionally.

Run:

git diff --cached

Confirm only intended Day 11 files are included.

PART 36 — COMMIT AND PUSH

Only after:

- Family Thread works
- return skill persists
- completion persists
- editing invalidates old completion
- Home summary works
- English works
- Arabic works
- RTL works
- flutter analyze passes
- flutter test passes
- privacy review passes

commit the Day 11 work.

Use branch:

feature/day-11-family-thread

Use descriptive commit messages such as:

Add local Family Thread reciprocal exchange

Persist Family Thread return skill and completion

Add bilingual Family Thread tests and documentation

Push the branch.

Guide me through opening the pull request.

Do not merge automatically.

FINAL DAY 11 VERIFICATION

Verify all of the following:

1. Naseej still launches on Android.
2. English works.
3. Arabic works.
4. RTL works.
5. Existing profile persistence works.
6. Teach a Skill still works.
7. Skill Draft persistence still works.
8. Optional speech-to-text still works.
9. Optional context photo still works.
10. Context photo remains local.
11. Firebase initialization still works.
12. App Check still works.
13. AI readiness still works.
14. Skill Card AI generation still works.
15. Offline Skill Card fallback still works.
16. Day 9 learner practice still works.
17. Three-step progress still persists.
18. Teach-back still persists.
19. Day 10 Listen / Stop / Replay still works.
20. TTS failure still does not block learning.
21. Completing the original lesson reveals the Family Thread.
22. Family Thread shows the teacher and learner connection.
23. Family Thread shows the taught skill.
24. Family Thread shows the learner's existing teach-back.
25. Family Thread shows the saved reciprocal suggestion.
26. Use Naseej Suggestion fills the return-skill field.
27. Return-skill text can be edited.
28. Return-skill response autosaves locally.
29. Completing Family Thread records the completion state.
30. Family Thread completion persists after reopening.
31. Family Thread completion banner appears.
32. Editing a completed return skill invalidates the old completion.
33. Home shows the completed Family Thread summary.
34. Family Thread works offline.
35. No Day 11 Gemini call was added.
36. No Family Thread response is sent to AI.
37. No Family Thread response is uploaded to Firebase.
38. No private photo is uploaded.
39. flutter gen-l10n succeeds.
40. flutter analyze passes.
41. flutter test passes.
42. No secrets are committed.
43. No real family responses are committed.
44. Day 11 AI assistance is documented.
45. AI is accurately described as a supporting tool rather than the creator of the full application.
46. I understand the Family Thread architecture and can explain it during competition Q&A.

Do not jump ahead to Day 12.

Day 11 is only the local Family Thread reciprocal family-exchange experience built on top of the completed learner lesson.


CONTINUATION REQUEST

Continue helping me complete Day 11 from the current implementation.

Do not restart the architecture or replace working code unnecessarily.

If tests fail, inspect the actual failure first.

Pay special attention to Flutter ListView widget tests, because Family Thread widgets may be outside the built viewport.

Use stable keys and scrollUntilVisible before accessing off-screen widgets.

If a completion banner is inserted above the current scroll position, scroll upward before asserting it.

Prefer checking localized labels through AppLocalizations rather than hardcoding duplicated English or Arabic wording.

Do not change production app logic merely to satisfy a test unless the application behavior itself is actually wrong.

Keep the Family Thread local, private, bilingual, offline-capable, and independent from any new AI request.

At the end, help me verify flutter analyze and flutter test both pass, then review Git changes before I commit and push.
