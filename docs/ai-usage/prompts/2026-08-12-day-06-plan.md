Assume that all of Day 5 steps are complete and verified for the Naseej Flutter app.

The project is part of the SMAC 2026 Project Development and the team name is Technova.

Project name:
Naseej

GitHub repository:
axbh25/smac2026-Technova-naseej

Day 5 is complete.

The app currently supports:

Welcome
→ Profile Setup
→ locally saved profile
→ Home
→ Teach a Skill
→ locally saved Skill Draft
→ saved draft restoration
→ optional speech-to-text input for the skill explanation

The app already supports:

- English and Arabic localization
- RTL
- persistent language selection
- persistent family profile
- persistent SkillDraft
- teacher and learner roles
- four skill categories
- editable multiline skill explanation
- optional English and Arabic speech-to-text
- manual typing as a fallback
- microphone permission/error fallback
- no microphone recording storage
- automated tests
- Android emulator verification
- local-only persistence

For Day 6, I want to add an optional context photo to the Teach a Skill draft.

The intended flow is:

Home
→ Teach a Skill
→ optionally capture a context photo
→ preview the photo
→ complete/edit the existing skill draft
→ save locally
→ return to Home
→ reopen the saved draft with the photo still available

The context photo must be optional.

A user who denies camera permission, whose camera is unavailable, or who simply does not want to attach a photo must still be able to complete and save the SkillDraft normally.

I am a beginner, so give me extremely clear, chronological, baby-step instructions.

Tell me:

- exactly which folder or file to open
- exactly which file to create
- exactly what code to paste
- whether to replace an entire file or only a section
- exactly which PowerShell commands to run
- exactly how to verify each important step
- exactly what emulator behavior I should observe

Do not skip intermediate verification.

==================================================
DAY 6 MAIN GOAL
==================================================

By the end of Day 6, Naseej should support one optional local context photo for a Teach a Skill draft.

The user should be able to:

1. Open Teach a Skill from Home.
2. See an optional Context Photo section.
3. Tap a button to open the device camera.
4. Capture one photo.
5. Return to Naseej.
6. See a preview of the captured photo.
7. Replace the photo if desired.
8. Remove the photo if desired.
9. Continue filling the existing Teach a Skill form.
10. Use voice input or manual typing as before.
11. Save the SkillDraft with or without a photo.
12. Return to Home.
13. See that the saved draft has an attached context photo when one exists.
14. Reopen the draft.
15. See the previously saved photo again.
16. Restart the app and still recover the saved photo.
17. Continue normally if camera permission is denied.
18. Continue normally if image capture fails.
19. Use the feature in English and Arabic.
20. Keep all Day 2–Day 5 functionality working.

The photo must never be required to enable Save Draft.

==================================================
GIT WORKFLOW
==================================================

Use the Day 6 feature branch:

feature/day-06-context-photo

Give me the exact Git commands needed to:

- switch to main
- pull the latest main
- create or switch to the Day 6 branch
- push the branch
- verify the current branch

From Day 3 onward, Abdullah performs all VS Code, Flutter, PowerShell, and Git development.

Do not require Shoug to run VS Code, Flutter, PowerShell, or Git commands.

==================================================
PRIVACY-FIRST REQUIREMENTS
==================================================

The Day 6 photo feature must remain privacy-first.

Naseej may store:

- one optional context photo selected/captured by the user
- the local reference required to reopen that photo with the saved SkillDraft

Naseej must NOT deliberately:

- upload the photo to a backend
- upload the photo to Firebase
- send the photo to an AI model
- send the photo to a cloud vision API
- attach the photo to speech-recognition requests
- store the photo in the Git repository
- add the photo to AI-usage documentation
- include real family photos in screenshots committed to the repository
- continuously access the camera
- capture photos without explicit user action

The photo is context for the local skill draft only.

Do not claim image metadata is removed unless the implementation actually removes it.

Do not claim the image is encrypted unless the implementation actually encrypts it.

==================================================
DAY 6 DEPENDENCIES
==================================================

Use a standard Flutter camera/image-picker package.

Prefer:

image_picker

for the MVP unless there is a strong reason not to.

Also use:

path_provider

if needed to obtain an application-private persistent directory.

Give me the exact commands, for example:

flutter pub add image_picker

and:

flutter pub add path_provider

Then run:

flutter pub get

Do not add unnecessary packages.

==================================================
ANDROID CAMERA PERMISSION
==================================================

Update the Android configuration for camera access.

Add only the permission required for this feature.

Use:

android.permission.CAMERA

Place it correctly in:

android/app/src/main/AndroidManifest.xml

Do not request unrelated permissions such as:

- location
- contacts
- phone
- SMS

Do not add broad external-storage permissions merely to save the app's own private context photo.

The photo should be copied into Naseej's private application storage.

==================================================
PHOTO STORAGE STRATEGY
==================================================

Do not rely permanently on the temporary file returned by the image picker.

After a successful camera capture:

1. receive the temporary image
2. create or locate a Naseej-private application directory
3. copy the selected image into private persistent application storage
4. use a unique application-controlled file name
5. retain only the private copy as the saved context photo

The draft must remain usable after the temporary picker file disappears.

Do not save photos into a publicly shared folder for this MVP.

Do not use Firebase Storage.

Do not use cloud storage.

==================================================
PHOTO ARCHITECTURE
==================================================

Keep the implementation small and testable.

Create a simple photo abstraction rather than calling ImagePicker and file-system APIs throughout the UI.

Use a structure similar to:

lib/
└── core/
    └── photo/
        ├── context_photo_service.dart
        └── image_picker_context_photo_service.dart

or an equally simple equivalent.

Do not over-engineer the feature.

The abstraction should support the operations required by Day 6, such as:

- capture a context photo
- persist/copy it into private app storage
- remove a previously stored photo

Production code should use the real image picker.

Tests should not require a real camera.

==================================================
PHOTO RESULT / FAILURE BEHAVIOR
==================================================

The photo service must safely handle:

- user cancels camera
- camera permission denied
- camera unavailable
- picker returns no file
- image copy fails
- existing image is missing
- replacement operation fails

These conditions must not destroy the user's existing SkillDraft form.

The user must always retain access to:

- learner nickname
- learner role
- category
- explanation
- voice input
- manual typing
- Save Draft

==================================================
SKILLDRAFT MODEL
==================================================

Extend:

lib/features/skill/domain/skill_draft.dart

Add one optional context-photo field.

Use a nullable value.

A draft without a photo remains valid.

For example, the model may contain a field conceptually similar to:

String? contextPhotoPath

Use the exact field name consistently throughout the implementation.

Update:

- constructor
- toJsonString()
- fromJsonString()
- equality
- hashCode

Old Day 4/Day 5 SkillDraft JSON that does not contain a photo field must still restore successfully.

Do not make the new field mandatory.

==================================================
PHOTO PATH PRIVACY
==================================================

The locally stored photo reference may exist inside local application state/storage, but documentation and AI prompt records must not contain real device file-system paths that expose personal user names.

Do not paste real private file-system paths into:

- AI prompt records
- development logs
- testing documentation
- Git commit messages

Use generic examples when documentation requires a path explanation.

==================================================
PERSISTENCE
==================================================

Continue using the existing SkillDraft JSON persistence.

When a SkillDraft containing a context photo is saved:

- store the local photo reference in the SkillDraft JSON
- keep the actual image file inside private app storage

When the app restarts:

- restore the SkillDraft
- restore the optional photo reference
- display the photo if the private file still exists

If the stored file reference exists but the file itself is missing:

- do not crash
- treat the photo as unavailable
- keep the rest of the SkillDraft

==================================================
PHOTO LIFECYCLE
==================================================

Avoid unnecessary orphaned images.

When the user replaces an existing context photo:

- successfully persist the new image first
- then safely remove the old private image where appropriate

When the user explicitly removes the photo:

- remove the photo reference from the draft form
- remove the private file where appropriate

Do not delete the current photo before a replacement is successfully captured and stored.

If replacement fails, preserve the existing photo.

==================================================
APP CONTROLLER
==================================================

Keep AppController responsible for application persistence state.

Do not put temporary camera UI state into AppController unless it is genuinely application-level state.

Continue using AppController to save the final SkillDraft.

The optional photo reference should persist as part of that draft.

Do not mix photo capture code directly into the persistence controller if a small photo service can keep responsibilities clearer.

==================================================
FIGMA — DAY 6 CONTEXT PHOTO
==================================================

Update the Teach a Skill design in Figma for English and Arabic.

Do not redesign the entire screen.

Add an optional Context Photo section that matches the existing Naseej design system.

Use the existing:

- colors
- typography
- spacing
- card radii
- button styles
- 48 × 48 minimum touch targets
- RTL conventions

Create useful states such as:

1. No photo / idle
2. Photo attached
3. Camera unavailable or permission denied

English concept:

Context photo

Optional

Add photo

Take a photo to show helpful visual context for this skill.

Arabic should contain natural localized equivalents.

The photo must be visually marked as optional.

==================================================
PHOTO EMPTY STATE
==================================================

When no photo exists, show a compact context-photo card.

Include:

- image/camera icon
- optional label
- explanatory text
- Add Photo or Take Photo action

The card should not imply that the photo is required.

==================================================
PHOTO ATTACHED STATE
==================================================

When a photo has been captured:

show:

- image preview
- Replace Photo action
- Remove Photo action
- small local/privacy message if appropriate

The image preview should:

- use a sensible fixed or constrained height
- preserve aspect ratio
- use BoxFit.cover where appropriate
- use rounded clipping consistent with Naseej
- not create layout overflow

Use a stable widget key for the preview, for example:

context_photo_preview

==================================================
PHOTO BUTTON TEST KEYS
==================================================

Use stable keys for automated tests.

Use concepts such as:

context_photo_button

context_photo_preview

remove_context_photo_button

If a fallback/error message is displayed, use a stable key such as:

context_photo_fallback_message

Use the exact names consistently in tests.

==================================================
LOCALIZATION
==================================================

Update:

lib/l10n/app_en.arb

and:

lib/l10n/app_ar.arb

Add all Day 6 photo strings.

Include concepts for:

- Context Photo
- Optional
- Add Photo / Take Photo
- Replace Photo
- Remove Photo
- photo description/helper text
- camera unavailable
- camera permission denied
- capture failed
- photo stored locally
- typing/draft remains available if camera cannot be used

Do not hard-code user-facing English or Arabic strings in the widgets.

Run:

flutter gen-l10n

after editing ARB files.

==================================================
TEACH SKILL SCREEN
==================================================

Update:

lib/features/skill/presentation/teach_skill_screen.dart

Preserve all Day 4 and Day 5 functionality.

The screen must continue to support:

- teacher summary
- learner nickname
- learner role
- categories
- Voice Input section
- explanation TextField
- Save Draft validation
- local draft persistence
- Arabic RTL

Add the Context Photo section without rewriting unrelated working code.

==================================================
PHOTO STATE IN TEACHSKILLSCREEN
==================================================

TeachSkillScreen should track the currently selected context photo.

If:

initialDraft

contains a saved photo reference, initialize the screen with that existing photo.

Track enough state to distinguish:

- no photo
- existing saved photo
- newly selected photo
- operation in progress
- recoverable failure

Do not make photo state affect _canSave.

==================================================
CAPTURE PHOTO
==================================================

Create an async helper method to capture/select the photo.

Suggested behavior:

1. unfocus text input
2. stop speech recognition if currently listening
3. request camera capture through the photo service
4. if the user cancels, make no changes
5. if capture succeeds, persist/copy the image to private app storage
6. verify the widget is still mounted
7. update the selected photo state
8. refresh the UI

Do not use BuildContext across asynchronous gaps without a mounted check.

flutter analyze must have no:

use_build_context_synchronously

issues.

==================================================
VOICE AND CAMERA INTERACTION
==================================================

Do not leave the microphone actively listening when the camera is opened.

Before launching the camera:

stop active speech recognition.

Voice input must still work normally after returning from the camera.

Do not request microphone and camera access simultaneously.

==================================================
PHOTO PREVIEW
==================================================

Use the locally stored file for preview.

Handle a missing or unreadable image safely.

Do not use a force unwrap that can crash when the file disappears.

If the file is unavailable:

show the no-photo/fallback UI
and preserve all non-photo draft values.

==================================================
REMOVE PHOTO
==================================================

Allow the user to remove the optional context photo.

Removing it must:

- update the screen state
- remove the photo reference from the draft that will next be saved
- safely delete the private image where appropriate

The rest of the form must remain unchanged.

==================================================
SAVE DRAFT
==================================================

Update _saveDraft() so the new SkillDraft includes the optional context-photo reference.

Continue stopping speech before saving.

Continue validating:

- learner nickname
- learner role
- category
- explanation minimum length
- explanation maximum length

Do NOT add:

photo != null

to _canSave.

The photo is optional.

Save Draft must work when the photo is null.

==================================================
SAFEAREA FIX
==================================================

When using:

SafeArea.minimum

remember that the property expects:

EdgeInsets

not:

EdgeInsetsDirectional

Use a compatible value such as:

const EdgeInsets.fromLTRB(
  AppSpacing.lg,
  AppSpacing.sm,
  AppSpacing.lg,
  20,
)

Do not pass EdgeInsetsDirectional to SafeArea.minimum.

==================================================
HOME SCREEN
==================================================

Update the saved Skill Draft summary on Home if appropriate.

If the saved draft has a valid context photo:

show a small photo indicator or thumbnail consistent with the Day 6 design.

If it has no photo:

keep the existing Day 4/Day 5 saved-draft design working.

Do not make Home dependent on the photo existing.

==================================================
RTL
==================================================

Verify the new Context Photo UI in Arabic.

Check:

- section heading
- optional label
- helper text
- camera action
- Replace Photo
- Remove Photo
- error/fallback text
- preview layout

Do not blindly mirror the image itself.

Directional controls/text should respect RTL naturally.

==================================================
FAKE PHOTO SERVICE
==================================================

Create a fake photo service for automated tests.

For example:

test/support/fake_context_photo_service.dart

It should allow tests to control:

- successful capture
- user cancellation
- capture failure
- permission/unavailable failure
- returned persistent fake path
- remove calls

Do not use the physical camera in widget tests.

Do not use personal family photographs in tests.

Use a generated/test fixture or abstract fake path only.

==================================================
SKILLDRAFT UNIT TESTS
==================================================

Extend:

test/skill_draft_test.dart

Test at least:

1. SkillDraft with no context photo survives JSON round trip.
2. SkillDraft with a photo reference survives JSON round trip.
3. Older JSON without the photo property still restores.
4. Existing damaged-JSON tests still pass.
5. Existing unknown-category validation still passes.
6. Existing explanation validation still passes.

Do not remove Day 4 tests.

==================================================
PHOTO SERVICE TESTS
==================================================

Add tests appropriate to the new photo abstraction.

Verify at least:

- successful captured image produces a private stored reference
- cancellation returns no new photo
- failure does not produce a saved photo
- remove delegates correctly

Do not depend on the emulator camera for automated unit tests.

==================================================
WIDGET TEST PROVIDERS
==================================================

Update:

test/widget_test.dart

so the test application can receive a fake context-photo service in addition to:

- AppController
- SpeechController

If the production UI reads the photo service through Provider, inject the fake through the test MultiProvider.

Existing tests that do not care about photos should receive a safe default fake.

Do not break the existing speech tests.

==================================================
PHOTO WIDGET TESTS
==================================================

Add widget tests for at least:

1. Teach a Skill shows the Context Photo section.
2. Photo is optional and Save Draft validation still depends only on the original required fields.
3. Successful capture displays context_photo_preview.
4. Capturing a photo does not erase learner/explanation values.
5. Removing the photo removes the preview.
6. Camera cancellation preserves the existing form.
7. Camera failure/permission denial shows fallback UI.
8. Camera failure does not disable manual form completion.
9. A saved draft with a context photo restores the preview.
10. Existing voice input tests still pass.
11. Arabic Teach a Skill remains RTL.

Use fake services only.

==================================================
PERMISSION-DENIED BEHAVIOR
==================================================

Camera permission denial must not block the workflow.

If permission/access is denied:

- show a clear localized fallback message
- do not crash
- preserve all existing form fields
- preserve the existing context photo if this was a replacement attempt
- keep Voice Input available
- keep manual typing available
- keep Save Draft available once normal required fields are valid

==================================================
QUALITY CHECKS
==================================================

After implementation run:

flutter gen-l10n

dart format lib test

flutter analyze

flutter test

Do not continue until:

flutter analyze

reports:

No issues found!

and:

flutter test

reports:

All tests passed!

Fix:

- errors
- warnings
- analyzer info messages
- layout overflows
- BuildContext async warnings
- unused imports
- invalid EdgeInsets types

Do not hide real failures by weakening tests.

==================================================
ANDROID EMULATOR VERIFICATION
==================================================

Run the app on the Android emulator.

Verify the English flow:

1. Open Home.
2. Open Teach a Skill.
3. Fill some existing form values.
4. Scroll to Context Photo.
5. Tap Add Photo.
6. Grant camera permission if requested.
7. Capture a safe non-personal test image.
8. Return to Naseej.
9. Confirm the preview appears.
10. Confirm existing form values remain.
11. Test Replace Photo.
12. Test Remove Photo.
13. Capture another safe test image if needed.
14. Complete the required form.
15. Save Draft.
16. Confirm Home works.
17. Reopen the draft.
18. Confirm the photo remains available.

==================================================
ARABIC EMULATOR VERIFICATION
==================================================

Verify Arabic:

1. switch to Arabic
2. confirm RTL
3. open Teach a Skill
4. locate Context Photo
5. confirm Arabic photo labels
6. capture a safe test photo
7. verify preview
8. verify Replace/Remove actions
9. verify voice input still works
10. verify manual typing still works
11. save
12. reopen
13. verify the photo persists

==================================================
APP RESTART VERIFICATION
==================================================

After saving a photo-containing draft:

1. fully restart the app
2. confirm profile restoration
3. confirm locale restoration
4. confirm SkillDraft restoration
5. confirm context-photo restoration
6. reopen the draft
7. confirm the preview appears
8. confirm the explanation and other values remain unchanged

==================================================
CAMERA FAILURE VERIFICATION
==================================================

If practical, manually test:

- permission denied
- user cancels camera

Confirm in both cases:

- app does not crash
- existing draft fields remain
- user can type
- user can use voice input
- user can save without a photo

==================================================
DAY 6 SCREENSHOT EVIDENCE
==================================================

Create:

docs/testing/screenshots/day-06/

Capture safe screenshots demonstrating the UI.

Use only non-personal test imagery.

Useful evidence includes:

- English Context Photo empty state
- English photo-attached state
- English saved/reopened draft
- Arabic Context Photo state
- Arabic photo-attached state
- fallback state if practical

Do NOT commit:

- personal family photos
- faces belonging to private individuals
- microphone recordings
- images containing private documents
- private addresses or identifying information

Use a harmless object or neutral scene for photo testing.

==================================================
DAY 6 DOCUMENTATION
==================================================

Document the Day 6 implementation accurately.

Include:

- optional context-photo behavior
- image-picker dependency
- private application storage
- photo lifecycle
- camera permission
- cancellation behavior
- permission-denied behavior
- no cloud photo upload
- no AI photo transmission
- SkillDraft photo persistence
- photo restoration
- English/Arabic support
- RTL
- automated tests
- emulator verification

Do not paste real absolute file-system paths containing personal names into the documentation.

Use generic examples instead.

==================================================
AI USAGE RECORD
==================================================

At the end of Day 6 create:

docs/ai-usage/prompts/2026-08-12-day-06-plan.md

Paste this exact Day 6 user prompt into that file.

Do not include:

- personal family photos
- file-system paths containing personal names
- passwords
- API keys
- tokens
- private Firebase values
- credentials
- microphone recordings
- private image contents

==================================================
GIT / REPOSITORY SAFETY
==================================================

Before committing:

git status

Carefully review every new file.

Do not accidentally stage captured camera images.

Do not commit the application's private photo-storage directory.

Do not commit temporary image-picker files.

Do not commit personal test images.

Use only safe UI screenshots as evidence.

Use sensible Day 6 commit checkpoints rather than one giant commit.

Push:

feature/day-06-context-photo

Prepare the Day 6 branch for a pull request into main.

Do not require Shoug to perform Git or VS Code work.

==================================================
DO NOT DO THESE THINGS
==================================================

Do not:

- make context photos mandatory
- upload context photos
- send context photos to AI
- add Firebase Storage
- add a backend
- add authentication
- add user accounts
- add cloud vision APIs
- implement Learn a Skill yet
- store temporary picker paths as the only persistent image copy
- delete an existing photo before a replacement succeeds
- break voice input
- break manual typing
- break SkillDraft persistence
- break profile persistence
- break locale persistence
- break Arabic RTL
- remove existing automated tests
- use real family photographs in repository tests/evidence
- expose personal device paths in AI documentation
- hide camera failures
- weaken validation to make tests pass

==================================================
RESPONSE FORMAT
==================================================

Give me Day 6 chronologically from beginning to end.

Number every major step.

For each coding step:

- give the exact file path
- say whether to create or edit the file
- say whether to replace the entire file or only a specific section
- provide complete code where appropriate
- give exact Windows PowerShell commands
- include verification immediately after important changes

Keep everything beginner-friendly.

Do not jump ahead.

The final Day 6 result should be a tested bilingual optional context-photo feature for Teach a Skill, with the image copied into private app storage, the SkillDraft still fully usable without camera access, existing voice/manual input preserved, and no photos uploaded to cloud or AI services.

Assume that all of Day 11 steps are complete and working correctly.

I am continuing the SMAC 2026 Naseej Flutter project for Team Technova.

I am a beginner with Flutter, Android, Firebase, Git, GitHub Actions, testing,
state management, local persistence, and mobile development, so guide me
carefully and do not skip important implementation or verification steps.

IMPORTANT COMPETITION RULE

AI is being used only as a documented supporting tool for planning,
explanations, debugging, localization guidance, wording assistance, testing
guidance, and isolated code assistance.

AI must not replace student development of the complete application.

I must understand the application architecture, data flow, privacy decisions,
reliability behavior, demo tools, tests, and Firebase boundaries well enough
to explain them during judging and Q&A.

When helping with code:

- Explain what the change does.
- Keep the architecture understandable.
- Avoid unnecessary complexity.
- Preserve working Days 1–11 features.
- Do not redesign or regenerate the entire project.
- Help me run and verify every important change myself.

IMPORTANT WORKING RULES

- Give me clear and concrete steps.
- Tell me the exact file path whenever I need to create or edit a file.
- When a Dart file needs substantial changes, provide the complete corrected
  file rather than scattered snippets where practical.
- Give PowerShell commands clearly.
- Assume the project root is:

  C:\dev\smac2026\naseej

- Do not make unrelated refactors.
- Preserve everything already working from Days 1–11.
- Keep the app bilingual English/Arabic.
- Preserve Arabic RTL.
- Keep private family data local-first.
- Day 12 must not add another Gemini feature.
- Do not upload learner progress.
- Do not upload Family Thread responses.
- Do not upload private context photos.
- Do not introduce Firestore, authentication, or Cloud Storage for Day 12.
- Do not expose API keys, Firebase credentials, passwords, App Check debug
  tokens, service-account keys, or other secrets.
- Do not place real family content, real context photographs, real teach-back
  responses, real reciprocal-skill responses, or personal family information
  in source code, tests, documentation, screenshots intended for GitHub, or
  Git history.
- Demo fixtures must contain synthetic sample information only.
- From Day 3 onward, only I perform VS Code, Flutter, PowerShell, Git,
  Firebase CLI, and GitHub work. Do not instruct Shoug to run coding or Git
  commands.

CURRENT PROJECT

App:
Naseej

Team:
Technova

Repository:
axbh25/smac2026-Technova-naseej

Local project root:
C:\dev\smac2026\naseej

Android package:
com.axbh25.naseej

Current application includes:

- Flutter Android application
- Provider / ChangeNotifier
- SharedPreferencesAsync/local persistence
- local JSON serialization
- English localization
- Arabic localization
- RTL support
- local family profile
- Teach a Skill draft
- typed explanation
- optional speech-to-text
- optional private context photo
- Firebase Core
- Firebase App Check
- Firebase AI Logic
- AI readiness check
- structured Gemini Skill Card generation
- deterministic Offline Guide
- Skill Card validation
- teacher review before saving
- three-step learner practice
- local LearningProgress
- teach-back
- lesson completion
- optional device text-to-speech
- Listen / Stop / Replay
- graceful TTS fallback
- reciprocal Family Thread
- return-skill response
- local Family Thread completion

DAY 12 GOAL

Day 12 is reliability, recovery, demo readiness, and automated verification.

The application already has its main MVP experience.

Do NOT use Day 12 to add another major user-facing feature.

Instead, make Naseej safer and easier to demonstrate during the SMAC
competition.

The Day 12 work should focus on four things:

1. Detecting and safely repairing inconsistent local application data.
2. Providing clearly labeled synthetic demo data for reliable judging demos.
3. Providing a safe local-data reset capability.
4. Adding automated GitHub verification for formatting, analysis, tests, and
   an Android build.

The result should make the application resilient during repeated development
runs, Android restarts, offline demonstrations, and competition judging.

DAY 12 PRINCIPLE

Day 12 should improve reliability without weakening Naseej's privacy model.

The app must remain:

- local-first
- bilingual
- offline-capable
- transparent about AI
- transparent about Offline Guides
- free of private family content in repository demo fixtures

PART 1 — DAY 12 BRANCH

Use:

feature/day-12-reliability-demo

Start from the completed Day 11 work.

Do not mix future Day 13 features into this branch.

Before committing Day 12, verify:

- formatting
- localization generation
- flutter analyze
- flutter test
- Android build
- privacy review

PART 2 — UNDERSTAND THE LOCAL JOURNEY DEPENDENCIES

Naseej's local data represents a sequence.

Conceptually:

profile
→ skill draft
→ reviewed Skill Card
→ learner progress
→ completed lesson
→ reciprocal Family Thread

Later data depends on valid earlier data.

For example:

- a draft must belong to the current profile
- a saved card must correspond to the current draft
- LearningProgress must match the current Skill Card fingerprint
- Family Thread completion must belong to the current valid learning journey

If earlier data becomes invalid, later dependent data must not continue to be
presented as valid.

PART 3 — SAFE STARTUP RECOVERY

Strengthen AppController initialization.

During initialization:

- read local values safely
- parse each value defensively
- check relationships between the restored objects
- preserve valid earlier information
- clear only invalid dependent later information

Do not delete all user data simply because one later object is malformed or
stale.

Example:

valid profile
valid draft
invalid card

should keep:

profile
draft

and clear:

card
progress / Family Thread data that depends on that card

PART 4 — LEARNING PROGRESS RECOVERY

If restored LearningProgress does not match the restored Skill Card:

- do not apply it
- clear the incompatible progress
- preserve the valid profile, draft, and card

If the saved progress JSON itself is malformed:

- do not crash
- treat it as unavailable
- continue loading earlier valid journey data

PART 5 — DEPENDENCY CASCADE

Make the cleanup rules explicit and deterministic.

Examples:

No valid profile:
→ no valid draft
→ no valid card
→ no valid progress

No valid draft:
→ no valid card
→ no valid progress

No valid card:
→ no valid learning progress / Family Thread

Invalid progress:
→ valid earlier profile/draft/card may remain

Do not silently reconstruct personal family content.

PART 6 — RECOVERY STATE

Expose enough controller state for the UI to know whether startup recovery
occurred.

Useful states may include concepts such as:

- normal
- repaired
- storage unavailable

Keep this simple.

Do not create an unnecessarily complex error framework.

PART 7 — REPAIRED DATA MESSAGE

If Naseej repaired inconsistent local data during initialization, show a clear
non-alarming notice.

English concept:

Local data repaired

Body concept:

Naseej found information that no longer matched the current family journey.
Valid information was kept and invalid later steps were cleared.

The notice should be dismissible.

Use localization.

PART 8 — STORAGE FAILURE STATE

If local storage itself cannot safely be read or written:

- keep the application open where possible
- do not crash
- expose a storage-unavailable status
- tell the user that changes may not survive a restart

Use wording such as:

Local storage is unavailable

Naseej could not safely read or save local family data. The app remains open,
but changes may not survive a restart.

Do not pretend data was successfully persisted if storage failed.

PART 9 — DEMO FEATURE AREA

Create a Day 12 demo feature under:

lib/features/demo/

Use a structure such as:

lib/features/demo/domain/
lib/features/demo/presentation/

Keep demo-specific synthetic journey construction out of unrelated production
screens where practical.

PART 10 — DEMO JOURNEY MODEL / FACTORY

Create a simple deterministic way to construct synthetic demo journeys.

Possible file:

lib/features/demo/domain/demo_journey.dart

The fixture must contain synthetic, non-private information.

Do NOT copy:

- real family names
- real family explanations
- real context photographs
- real teach-back responses
- real Family Thread responses

into the repository.

Demo data must be obviously sample data.

PART 11 — TWO DEMO MODES

Provide two different demo setups.

MODE A — AI-READY SAMPLE

This should load only the local information needed to demonstrate the normal
AI generation flow.

It may create:

- synthetic local profile
- synthetic reviewed draft

It must NOT insert a fake AI-generated result.

After loading this sample, I should continue through the normal Skill Card
screen and press the real:

Generate with AI

action if network/Firebase AI is being demonstrated.

The demo screen must make clear that this sample contains no generated result.

PART 12 — COMPLETED OFFLINE SAMPLE

Provide a second sample that demonstrates the later application screens without
requiring Wi-Fi.

This may create:

- synthetic profile
- synthetic draft
- deterministic Offline Guide Skill Card
- completed learner progress
- synthetic teach-back
- synthetic return-skill response
- completed Family Thread timestamps

The Skill Card origin must be:

Offline Guide

It must NOT be presented as a live Gemini result.

This allows judges to see the complete later experience if cloud access is
unavailable.

PART 13 — DEMO & LOCAL DATA SCREEN

Create:

lib/features/demo/presentation/demo_data_screen.dart

Use a stable key:

demo_data_screen

The screen should be clearly labeled:

Demo & Local Data

This should not look like part of the normal family lesson.

It is a development/demo reliability tool.

PART 14 — LOCAL JOURNEY STATUS

Show the status of local journey components.

Include concepts such as:

- Family profile
- Skill draft
- 3-Step Card
- Learner progress
- Family Thread

Display states such as:

Available
Missing
Completed

Use current AppController state.

Do not expose raw JSON.

PART 15 — DEMO PREPARATION SECTION

Add a section explaining why demo fixtures exist.

Concept:

Prepare a reliable demo

Explain that clearly labeled local sample data can be loaded without placing
private family information in the public repository.

PART 16 — AI-READY SAMPLE CARD

Add an AI-ready sample card.

Use clear text such as:

AI-ready sample

Loads a local profile and reviewed draft. Continue through the normal card
screen to make the real AI request.

Add button:

Load AI-Ready Sample

Use a stable key for tests.

For example:

load_ai_ready_demo_button

PART 17 — COMPLETED OFFLINE SAMPLE CARD

Add:

Completed offline sample

Explain that it loads a complete local Family Thread using an Offline Guide.

Add button:

Load Completed Offline Sample

Use stable key:

load_completed_demo_button

Include a clear notice:

This sample is an Offline Guide and is never presented as a live AI result.

PART 18 — REPLACING EXISTING LOCAL DATA

Loading demo data may replace the current local family journey.

Do not do that silently.

Before replacement, show a confirmation dialog.

Concept title:

Replace current local data?

Body:

The current profile, draft, card, progress, and Family Thread will be replaced
by clearly labeled sample data.

Add actions:

Cancel
Replace Data

PART 19 — RESET LOCAL FAMILY DATA

Add a reset section to the Demo & Local Data screen.

Concept title:

Reset local family data

Allow the user to remove:

- local profile
- draft
- saved card
- learner progress
- Family Thread
- current private context photo when possible

Keep the currently selected language.

Button:

Reset Local Family Data

Stable key:

reset_local_data_button

PART 20 — RESET CONFIRMATION

Reset must require explicit confirmation.

Use:

Reset all local family data?

Explain:

This removes the local family journey from this phone. This action cannot be
undone.

Add a stable confirmation key such as:

confirm_reset_data_button

Do not reset automatically.

PART 21 — CONTEXT PHOTO CLEANUP

When replacing/resetting a journey:

- attempt to remove the old private context photo if appropriate
- do not crash if deletion fails
- do not delete unrelated user files
- only operate on the app-controlled private context photo

If photo deletion fails, show a warning such as:

The previous private photo could not be deleted automatically.

PART 22 — ATOMIC / FAILURE-SAFE DATA OPERATIONS

Demo loading and reset should avoid leaving the controller in a half-updated
state.

If a local-data operation fails:

- preserve or restore the previous valid state when practical
- surface a clear error
- do not report success when only part of the operation completed

Use wording such as:

The local-data operation could not be completed. Existing data was restored
when possible.

PART 23 — DATA TOOLS ENTRY POINT

Add a small reusable access point for Demo & Local Data.

A suitable file is:

lib/core/widgets/data_tools_button.dart

Use a clear icon such as storage/database/developer-tools.

Use localization tooltip:

Demo and local data

Do not make this action easy to confuse with the normal Teach/Learn flow.

PART 24 — WELCOME / HOME ACCESS

Provide reasonable access to the Demo & Local Data screen from locations useful
during judging and testing.

For example:

- Welcome screen
- Home screen

Do not disrupt the main app layout.

Do not make demo-data loading happen automatically.

PART 25 — ENGLISH LOCALIZATION

Update:

lib/l10n/app_en.arb

Add all Day 12 strings required for:

- Demo & Local Data
- recovery messages
- storage unavailable message
- local journey status
- data labels
- present/missing/completed states
- AI-ready sample
- completed offline sample
- sample notices
- reset data
- confirmation dialogs
- operation success/error
- photo cleanup warning

Do not manually edit generated localization Dart files.

PART 26 — ARABIC LOCALIZATION

Update:

lib/l10n/app_ar.arb

Translate every Day 12 string naturally into Arabic.

Preserve RTL.

Do not leave untranslated Day 12 English strings.

After English and Arabic ARB updates, run:

flutter gen-l10n

The untranslated-message warning should be gone when both files are complete.

PART 27 — DEMO DATA TESTS

Add automated tests for synthetic demo journey creation.

Verify that:

- AI-ready sample does not contain a generated Skill Card
- completed sample uses Offline Guide origin
- completed sample contains valid completed learner progress
- completed sample contains a completed Family Thread
- fixtures contain deterministic synthetic content

Do not invoke Firebase.

PART 28 — RECOVERY TESTS

Add or extend AppController/storage tests.

Test cases should include combinations such as:

- all valid saved data
- invalid draft clears later dependent state
- invalid/mismatched card clears progress
- mismatched LearningProgress is discarded
- older valid data remains when later data is invalid
- malformed JSON does not crash
- storage failure exposes the correct status
- reset preserves selected locale

Use fake storage.

PART 29 — DEMO DATA SCREEN TESTS

Create or update:

test/demo_data_screen_test.dart

At minimum test:

1. completed local sample loads a full Family Thread
2. reset confirmation clears family data but keeps locale
3. Arabic Demo & Local Data screen uses RTL

Use:

412 × 892

as the test surface.

IMPORTANT:

DemoDataScreen is a long scrollable screen.

Flutter ListView children below the viewport may not be built yet.

Do NOT call:

tester.ensureVisible(...)

on a Finder currently matching zero widgets.

Instead use:

tester.scrollUntilVisible(...)

with the screen's Scrollable.

Use stable keys such as:

load_completed_demo_button
reset_local_data_button
confirm_reset_data_button

PART 30 — PRESERVE DAY 11 BEHAVIOR

Do not weaken existing tests.

Preserve:

- profile flow
- Teach a Skill
- speech input
- context photo
- AI readiness
- Skill Card generation
- Offline Guide
- learner practice
- teach-back
- TTS
- Family Thread
- English
- Arabic
- RTL

Day 12 reliability work must not break the existing MVP.

PART 31 — GITHUB ACTIONS

Add automated repository verification.

Create at the PROJECT ROOT:

.github/workflows/flutter-checks.yml

IMPORTANT:

It must be:

C:\dev\smac2026\naseej\.github\workflows\flutter-checks.yml

NOT:

lib\.github\workflows\flutter-checks.yml

GitHub only recognizes workflows under the repository-root .github/workflows
directory.

PART 32 — WORKFLOW TRIGGERS

Run the Flutter verification workflow for:

- pushes to main
- pushes to feature/** branches
- pull requests

Use read-only repository contents permission unless more is actually required.

Use concurrency cancellation so an outdated run can be stopped when a newer
commit is pushed.

PART 33 — WORKFLOW CHECKS

The workflow should run on:

ubuntu-latest

and perform:

1. repository checkout
2. Java setup
3. Flutter stable setup
4. flutter --version
5. flutter pub get
6. flutter gen-l10n
7. Dart format verification
8. flutter analyze
9. flutter test
10. flutter build apk --debug

Use an appropriate timeout.

The workflow should fail if any required quality check fails.

PART 34 — CURRENT GITHUB ACTION VERSIONS

Use current maintained major versions of official GitHub actions rather than
known deprecated versions.

Prefer:

actions/checkout@v5
actions/setup-java@v5

if compatible with the workflow environment.

Do not treat Node-runtime deprecation warnings as application failures, but
avoid unnecessary deprecated action versions when easy to update.

PART 35 — LOCAL FORMAT CHECK

Before pushing the workflow, run locally:

dart format lib test

Then:

dart format --output=none --set-exit-if-changed lib test

The second command should exit successfully.

PART 36 — LOCAL ANALYZER CHECK

Run:

flutter analyze

Fix every genuine analyzer issue.

Do not assume that passing Windows compilation guarantees Linux GitHub Actions
analysis will pass.

Pay attention to:

- imports
- path/file-name case
- generated localization state
- platform-specific assumptions

PART 37 — LOCAL TEST CHECK

Run:

flutter test

All existing and Day 12 tests must pass.

If a widget test reports:

Bad state: No element

inspect whether it is trying to access a lazy ListView child that has not yet
been built.

Use scrollUntilVisible before changing production code.

PART 38 — ANDROID BUILD CHECK

Run an Android build locally.

At minimum:

flutter build apk --debug

Also verify the release build when appropriate:

flutter build apk --release

Warnings that do not fail the build should be distinguished from actual build
errors.

Do not disable tree-shaking merely because Flutter reports that Material Icons
were tree-shaken successfully.

PART 39 — GITHUB ACTION FAILURE DEBUGGING

After pushing the workflow, inspect the GitHub Actions run.

If it fails:

- identify the exact failing step
- do not assume yellow warnings caused the red failure
- reproduce the failing command locally when possible

For example:

If Generate localizations passes,
Format passes,
Analyze source fails,

then investigate:

flutter analyze

first.

Tests and build being "skipped" after a failed analyze step is expected GitHub
Actions behavior.

PART 40 — RELIABILITY MANUAL TEST

On the Android emulator/device verify:

- normal valid data still restores
- synthetic AI-ready sample loads
- synthetic completed Offline sample loads
- completed sample shows Family Thread
- sample is clearly marked Offline Guide
- reset requires confirmation
- reset clears family journey data
- language remains selected after reset
- app remains usable after reset
- no private sample photograph is required

PART 41 — OFFLINE DEMO TEST

Enable Airplane Mode after the completed Offline sample is loaded.

Verify that I can still demonstrate:

- saved three-step card
- learner completion
- teach-back
- Family Thread
- return skill
- completed exchange
- local persistence

Do not claim the offline sample is an AI-generated live result.

PART 42 — AI DEMO TEST

For the AI-ready sample:

- load the synthetic profile and reviewed draft
- continue through the normal user flow
- use the actual Generate with AI action
- verify the data-boundary notice is still accurate
- verify the context photo is not sent
- review the generated card before saving

Do not pre-create a fake "AI result" in the fixture.

PART 43 — README / DOCUMENTATION

Update README and Day 12 documentation only as needed.

Document:

- reliability/recovery behavior
- synthetic demo data
- AI-ready sample
- completed Offline Guide sample
- local reset
- automated GitHub checks
- privacy boundary
- no additional Day 12 AI feature

Do not include secrets or private family content.

PART 44 — AI ASSISTANCE DOCUMENTATION

Record Day 12 AI assistance accurately.

AI may be documented as assisting with:

- planning
- reliability architecture discussion
- debugging
- localization wording
- test guidance
- GitHub Actions guidance
- documentation drafting

Do not state that AI independently generated or developed the entire
application.

The team must understand all committed code and be able to discuss it during
Q&A.

PART 45 — STORE THE EXACT DAY 12 PROMPT

Create:

docs/ai-usage/prompts/2026-08-16-day-12-plan.md

Store the exact user request that began Day 12 and this Day 12 plan.

Do not include:

- API secrets
- API keys
- App Check debug tokens
- passwords
- Firebase login credentials
- service-account credentials
- private family content
- real context photos
- real teach-back responses
- real return-skill responses
- other personal user data

RETAINED DAY 12 PLANNING NOTES

- Day 12 focuses on reliability and competition-demo readiness.
- The main MVP feature set was already completed through Day 11.
- Local data represents a dependency chain:
  profile → draft → card → progress → Family Thread.
- Startup recovery should preserve valid early-stage data while clearing
  incompatible later-stage data.
- Corrupted or stale local state must not crash the application.
- Storage failures should be visible rather than silently reported as success.
- Demo fixtures must use synthetic data.
- The AI-ready fixture contains no fake AI result.
- The completed demonstration fixture uses a deterministic Offline Guide.
- Offline sample output must remain clearly distinguishable from AI output.
- Demo data replacement requires confirmation.
- Local reset requires confirmation.
- Reset keeps the selected locale.
- The old private app-owned context photo should be removed when possible.
- Photo cleanup failure should not crash the data operation.
- Demo/local-data operations should avoid leaving half-written state.
- DemoDataScreen is long and widget tests should use scrollUntilVisible for
  lazy children.
- GitHub Actions should automatically check formatting, analysis, tests, and
  Android debug build.
- The GitHub workflow belongs at repository-root .github/workflows/.
- CI failures should be debugged by identifying the exact failing step.
- Yellow workflow deprecation warnings are separate from red command failures.
- Day 12 adds no new Gemini content feature.
- Learner data, Family Thread data, demo state, and context photos remain
  outside the Day 12 AI boundary.
- AI remains a documented supporting development tool.
- I must understand and explain all Day 12 changes.

PART 46 — PRIVACY REVIEW

Before staging, inspect tracked files for:

- API keys
- API secrets
- App Check debug tokens
- passwords
- Firebase credentials
- service-account files
- private family photographs
- real family explanations
- real teach-back responses
- real return-skill responses
- personal family information

Synthetic demo fixtures are allowed.

Make sure the synthetic fixtures cannot reasonably be mistaken for real family
content.

PART 47 — GIT REVIEW

Before staging run:

git status

Then:

git diff

Review every Day 12 file.

Check especially:

- demo fixture data
- localization
- AppController recovery logic
- reset logic
- workflow file
- documentation

Then stage intentionally.

Run:

git diff --cached

before committing.

PART 48 — GITHUB WORKFLOW VERIFICATION

Push the branch and allow GitHub Actions to run.

A successful workflow should complete:

- checkout
- Java setup
- Flutter setup
- dependencies
- localization generation
- format check
- analyze
- tests
- APK debug build

Do not consider Day 12 automation finished while the workflow still has a red
failure.

If the workflow exposes a real analyzer/test/build problem, fix that issue,
commit the correction, push, and allow the workflow to run again.

PART 49 — COMMIT AND PUSH

Only after:

- recovery works
- demo fixtures work
- reset works
- English works
- Arabic works
- RTL works
- flutter gen-l10n passes
- format verification passes
- flutter analyze passes
- flutter test passes
- Android build passes
- GitHub Actions passes
- privacy review passes

commit the Day 12 work.

Use:

feature/day-12-reliability-demo

Use meaningful commits such as:

Add local data recovery and demo journey tools

Add reliable offline demo and local reset flow

Add automated Flutter verification workflow

Document Day 12 reliability and demo safeguards

Push the branch.

Guide me through opening the pull request.

Do not merge automatically.

FINAL DAY 12 VERIFICATION

At the end verify:

1. Naseej launches on Android.
2. English works.
3. Arabic works.
4. RTL works.
5. Welcome still works.
6. Profile persistence still works.
7. Teach a Skill still works.
8. Speech-to-text remains optional.
9. Context photo remains optional and private.
10. Firebase initializes.
11. App Check works.
12. AI readiness works.
13. Gemini Skill Card generation still works.
14. Offline Guide still works.
15. Skill Card review still works.
16. Learner practice still works.
17. Learning progress persists.
18. Teach-back persists.
19. Device TTS remains optional.
20. TTS failure does not block learning.
21. Family Thread still works.
22. Family Thread completion persists.
23. Startup handles stale/incompatible downstream data.
24. Valid earlier journey data is preserved during repair.
25. Invalid dependent later data is cleared.
26. Recovery does not crash the app.
27. Storage failure is surfaced clearly.
28. Demo & Local Data screen opens.
29. Local journey status is displayed.
30. AI-ready synthetic sample loads.
31. AI-ready sample contains no fake generated result.
32. Real AI demonstration still uses the normal Generate with AI action.
33. Completed Offline sample loads.
34. Completed sample uses Offline Guide origin.
35. Completed sample contains a completed Family Thread.
36. Completed sample works without Wi-Fi.
37. Demo data contains no private family content.
38. Existing local data is not replaced without confirmation.
39. Reset requires confirmation.
40. Reset clears profile/draft/card/progress/Family Thread.
41. Reset keeps the selected locale.
42. Private context-photo cleanup is attempted safely.
43. Photo cleanup failure does not crash reset/replacement.
44. flutter gen-l10n passes.
45. Dart formatting check passes.
46. flutter analyze passes.
47. flutter test passes.
48. Android debug build passes.
49. Release APK can be built when required.
50. GitHub workflow is located under .github/workflows.
51. GitHub Actions workflow runs automatically.
52. GitHub Actions format check passes.
53. GitHub Actions analyze step passes.
54. GitHub Actions tests pass.
55. GitHub Actions Android build passes.
56. GitHub Actions has no unresolved red failure.
57. No secret is committed.
58. No App Check debug token is committed.
59. No private photograph is committed.
60. No real family response is committed.
61. Day 12 AI assistance is documented.
62. AI is accurately described as a supporting tool.
63. I understand the reliability/recovery architecture.
64. I can explain the difference between the AI-ready sample and completed
    Offline Guide sample during judging.
65. I can explain why Day 12 adds no new Firebase AI request.

Do not jump ahead to Day 13.

Day 12 is only reliability, safe local-data recovery, clearly labeled demo
preparation, reset tools, and automated project verification for the completed
Naseej MVP.