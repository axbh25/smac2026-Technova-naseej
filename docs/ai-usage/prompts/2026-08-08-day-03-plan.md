Assume that all of Day 2 steps are complete and verified for the Naseej Flutter app.

The project is part of the SMAC 2026 Project Development and the team name is Technova.

Project name:
Naseej

Local project path:
C:\dev\smac2026\naseej

GitHub repository:
axbh25/smac2026-Technova-naseej

The Day 2 bilingual Welcome screen, English/Arabic localization, RTL behavior, design tokens, tests, emulator verification, screenshots, and GitHub workflow are complete.

The app currently launches to the bilingual Welcome screen.

For Day 3, I want to build the first real user flow after Welcome:

Welcome
→ Profile Setup
→ save profile locally
→ Home

The Day 3 work must focus on local profile persistence and the first empty Home screen.

I am a beginner, so give me extremely clear, chronological, baby-step instructions. Tell me exactly which folder or file to open, exactly what to create, exactly what code to paste or replace, and exactly which commands to run.

Do not skip intermediate verification steps.

Use the existing project architecture and design system wherever possible rather than rebuilding things unnecessarily.

==================================================
DAY 3 GOALS
==================================================

By the end of Day 3, the app should:

1. Continue from the Welcome screen into Profile Setup.
2. Allow the user to enter a nickname.
3. Allow the user to choose one of four family roles:
   - Grandparent
   - Parent
   - Teen
   - Child
4. Keep the Save Profile button disabled until:
   - a nickname is present, AND
   - a role is selected.
5. Save the profile locally on the device.
6. Persist the selected app language locally.
7. Restore the saved language and profile after restarting the app.
8. Skip the Welcome/Profile Setup flow on later launches when a profile already exists.
9. Show the Home screen when a saved profile exists.
10. Support English and Arabic correctly, including RTL behavior.
11. Keep all Day 2 functionality working.
12. Include automated tests for the new flow.
13. Produce emulator screenshots and Day 3 documentation/evidence.

==================================================
GIT WORKFLOW
==================================================

Start from the latest main branch.

Create and use this Day 3 feature branch:

feature/day-03-profile-persistence

Give me the exact Git commands required to:

- switch to main
- pull the latest main
- create the Day 3 branch
- push the branch
- verify the current branch

From Day 3 onward, Abdullah will perform the VS Code, Flutter, and Git development work.

Do not require Shoug to run VS Code or Git commands.

==================================================
FIGMA — PROFILE SETUP
==================================================

Use the existing Naseej Figma file and established design system.

Do not redesign the existing Welcome screens.

Create the Profile Setup design in both English and Arabic.

Reference screen size:

412 × 892

English frame:

03_Screens_EN
→ 02_ProfileSetup_EN

Arabic frame:

04_Screens_AR
→ 02_ProfileSetup_AR

The Profile Setup design should include:

- header
- back button
- screen title
- compact language control
- introductory text
- nickname field
- role-selection heading
- four role cards
- Save Profile button

English title:

Create your local profile

Arabic title:

أنشئ ملفك المحلي

English introduction:

Choose a nickname and role. This information stays on this phone.

Arabic introduction:

اختر اسماً مختصراً ودورك في الأسرة. تبقى هذه البيانات على هذا الهاتف.

Nickname field:

English:
Nickname
For example, Fatima

Arabic:
الاسم أو اللقب
مثال: فاطمة

Roles:

Grandparent
جد أو جدة

Parent
أب أو أم

Teen
مراهق أو مراهقة

Child
طفل أو طفلة

Use Material icons:

Grandparent → elderly
Parent → family_restroom
Teen → school
Child → child_care

Role cards:

176 × 108

Arrange them in a two-column grid with:

12 px column gap
12 px row gap

Create reusable role-choice components with Default and Selected states.

Reuse the existing Button/Primary component for:

English:
Save profile

Arabic:
حفظ الملف

The Save button should visually start disabled.

Use deliberate RTL behavior for Arabic rather than blindly mirroring the English screen.

==================================================
FIGMA — EMPTY HOME
==================================================

Create the first empty Home design in both English and Arabic.

English:

03_Screens_EN
→ 03_Home_EN

Arabic:

04_Screens_AR
→ 03_Home_AR

Screen size:

412 × 892

Include:

- app header
- compact language action
- personalized greeting
- saved-profile summary
- empty family-weave state
- Teach a Skill button
- Learn a Skill button

Header title:

Naseej / نسيج

English greeting example:

Hello, Fatima

Arabic greeting example:

مرحبًا، فاطمة

Create:

Card/ProfileSummary

Specifications:

Width: 364
Minimum height: 88
Padding: 16
Gap: 12
Fill: color/surface
Border: color/border
Corner radius: 16

Include:

- saved role icon
- role label
- small lock icon

English message:

Saved on this phone.

Arabic:

محفوظ على هذا الهاتف

Create an empty family-weave card:

Width: 364
Minimum height: 270
Padding: 24
Fill: color/surface_soft
Corner radius: 24

English title:

Your family weave starts here

English body:

Teach one skill and learn one skill to create your first thread.

Arabic title:

يبدأ نسيج عائلتك من هنا

Arabic body:

علّم مهارة وتعلّم مهارة لتصنع أول خيط.

Use a simple thread/connection icon.

Primary action:

Teach a Skill
علّم مهارة

Secondary action:

Learn a Skill
تعلّم مهارة

Both buttons:

Width: 364
Height: 56
Corner radius: 16

The Teach/Learn buttons do not need real skill flows on Day 3.

For now they may provide an honest temporary “next build” message.

Mark the completed Day 3 Profile Setup and Home frames Ready for Development if the existing Figma status/annotation system supports it.

==================================================
FLUTTER ARCHITECTURE
==================================================

Keep the code organized by responsibility.

Add whatever folders/files are necessary for:

- local application state
- local storage abstraction
- SharedPreferences implementation
- family profile domain model
- Profile Setup presentation
- Home presentation
- reusable role-choice UI
- reusable compact language toggle

A reasonable structure should include concepts similar to:

lib/
├── core/
│   ├── state/
│   │   └── app_controller.dart
│   ├── storage/
│   │   ├── app_storage.dart
│   │   └── shared_preferences_app_storage.dart
│   └── widgets/
│       └── language_toggle_button.dart
├── features/
│   ├── profile/
│   │   ├── domain/
│   │   │   └── family_profile.dart
│   │   └── presentation/
│   │       ├── profile_setup_screen.dart
│   │       ├── family_role_ui.dart
│   │       └── widgets/
│   │           └── role_choice_card.dart
│   └── home/
│       └── presentation/
│           └── home_screen.dart

Do not create unnecessary architecture layers.

==================================================
LOCAL STORAGE
==================================================

Use local device storage, not a backend.

Use SharedPreferences for the Day 3 persistence layer.

Create an AppStorage abstraction so application state is not tightly coupled to SharedPreferences.

Persist:

- selected locale
- saved family profile

The family profile should contain:

- nickname
- selected family role

The profile must be serializable so it can be saved and restored.

Do not store:

- passwords
- authentication tokens
- private family information beyond the simple nickname/role required for this Day 3 demo
- Firebase secrets

No account should be required.

No location should be required.

==================================================
APP CONTROLLER
==================================================

Create a ChangeNotifier-based AppController to manage:

- current locale
- current saved profile
- loading stored state on startup
- changing locale
- saving profile
- clearing profile if needed

The app must initialize the controller before displaying the main UI.

The controller should restore both locale and profile from storage.

Changing language should update the UI immediately and persist the selection.

Saving a profile should notify listeners so the app can transition to Home.

==================================================
APP STARTUP / ROUTING
==================================================

Update the app so startup behavior is:

If no saved profile exists:

Welcome screen

If a saved profile exists:

Home screen

The Welcome Continue button should no longer show the old temporary SnackBar.

Instead, Continue should navigate to Profile Setup.

The Profile Setup Save button should:

1. remain disabled until nickname and role exist;
2. save the FamilyProfile through AppController;
3. return/transition so the app displays Home.

Do not add a routing package unless it is genuinely necessary.

MaterialPageRoute and simple app-state gating are sufficient for this stage.

==================================================
LOCALIZATION
==================================================

Continue using the existing ARB-based localization system.

Update:

lib/l10n/app_en.arb
lib/l10n/app_ar.arb

Add all Day 3 Profile Setup and Home strings.

Generate localization code using:

flutter gen-l10n

Arabic must display correctly in RTL.

The selected language must remain selected after the app restarts.

Do not hard-code user-facing English/Arabic strings inside widgets when they belong in localization resources.

==================================================
PROFILE SETUP IMPLEMENTATION
==================================================

Build ProfileSetupScreen according to the approved Figma design.

Requirements:

- key:
  profile_setup_screen

- nickname TextField key:
  nickname_field

- maximum sensible nickname length
- appropriate label and hint
- role grid
- four family roles
- selected state clearly visible
- Save Profile key:
  save_profile_button

The button must be disabled until both nickname and role are present.

Use the existing theme and colors.

Avoid layout overflow on the 412 × 892 reference device.

The role cards must support English and Arabic text without RenderFlex overflow.

==================================================
HOME IMPLEMENTATION
==================================================

Create the empty Home screen.

Use key:

home_screen

Read the saved profile from AppController.

Display the saved nickname dynamically.

Example English:

Hello, Fatima

Example Arabic:

مرحبًا، فاطمة

Show the user's stored role.

Show that the profile is stored locally.

Show the empty family-weave message.

Show:

Teach a Skill
Learn a Skill

and their Arabic translations.

These buttons should not pretend that the skill flows exist yet.

Use a clear temporary message indicating those flows will be added in a later build.

==================================================
MAIN.DART
==================================================

Update main.dart so it:

1. ensures Flutter binding is initialized;
2. creates the SharedPreferences-backed storage;
3. creates AppController;
4. awaits AppController initialization;
5. provides AppController to the widget tree using Provider/ChangeNotifierProvider;
6. launches NaseejApp.

Keep startup logic straightforward.

==================================================
DEPENDENCIES
==================================================

If required, add only the dependencies needed for Day 3.

Expected concepts include:

provider
shared_preferences

Explain exactly how to add them and run:

flutter pub get

Do not add unnecessary packages.

==================================================
TESTING
==================================================

Update automated widget tests.

Create a fake AppStorage for tests so tests do not depend on real SharedPreferences state.

Tests should cover at least:

1. English Welcome screen appears by default when no profile exists.
2. Switching to Arabic updates the UI and stores the locale.
3. Continue opens Profile Setup.
4. Save Profile remains disabled until both nickname and role exist.
5. Entering nickname alone does not enable Save.
6. Selecting a role as well enables Save.
7. Saving a profile opens Home.
8. The saved profile is written to storage.
9. A previously saved profile is restored at startup.
10. A previously saved Arabic locale is restored.
11. Arabic Home shows the expected greeting.
12. Basic Welcome accessibility guidelines still pass.

Use the 412 × 892 reference surface size when appropriate.

Fix real rendering overflows rather than hiding them from tests.

==================================================
QUALITY CHECKS
==================================================

After implementation, run:

flutter gen-l10n

dart format lib test

flutter analyze

flutter test

Do not continue until:

flutter analyze

reports:

No issues found!

and:

flutter test

reports:

All tests passed!

==================================================
EMULATOR VERIFICATION
==================================================

Run the app on the Android emulator.

Verify manually:

English:
- Welcome
- Profile Setup
- nickname entry
- role selection
- disabled/enabled Save button
- saved profile
- Home
- greeting
- profile summary
- empty weave state

Arabic:
- RTL layout
- Arabic Profile Setup
- Arabic nickname/role labels
- Arabic Home
- Arabic greeting
- language persistence

Also verify persistence:

1. select a language;
2. save a profile;
3. restart the app;
4. confirm the app restores the saved profile and language;
5. confirm it opens Home instead of Welcome.

==================================================
DAY 3 SCREENSHOT EVIDENCE
==================================================

Create:

docs/testing/screenshots/day-03/

Capture emulator screenshots for the completed Day 3 flow, including:

- English Profile Setup
- Arabic Profile Setup
- English Home
- Arabic Home

Use ADB screenshot commands if appropriate.

Give me exact Windows PowerShell commands.

==================================================
DAY 3 DOCUMENTATION
==================================================

Create/update the appropriate Day 3 documentation.

Include:

docs/design/day-03-profile-flow.md

Document the intended flow:

Welcome
→ Profile Setup
→ Local persistence
→ Home

Document:

- nickname
- roles
- local-only profile behavior
- language persistence
- empty Home state

Update the development/testing evidence for Day 3.

Keep documentation consistent with the actual implementation.

==================================================
AI USAGE RECORD
==================================================

At the end of Day 3, create:

docs/ai-usage/prompts/2026-08-08-day-03-plan.md

Store this Day 3 user prompt there exactly.

Do not include:

- passwords
- tokens
- private family information
- Firebase secrets
- credentials

==================================================
GIT / COMMITS
==================================================

Give me sensible commit checkpoints rather than one giant commit.

Before every commit, have me run:

git status

and review staged changes.

Use clear Day 3 commit messages.

At the end, push:

feature/day-03-profile-persistence

and prepare the work for a pull request into main.

Do not have Shoug perform Git or VS Code work.

==================================================
IMPORTANT CONSTRAINTS
==================================================

Do not:

- add authentication
- add accounts
- add Firebase
- add backend services
- collect location
- build Teach Skill flows yet
- build Learn Skill flows yet
- create unnecessary architecture
- break the Day 2 Welcome screen
- remove English/Arabic localization
- remove RTL support
- hard-code secrets
- skip testing
- skip emulator verification
- skip documentation
- skip Git evidence

Use the existing Naseej theme and design tokens.

Keep the implementation simple, maintainable, and appropriate for a beginner team.

==================================================
RESPONSE FORMAT
==================================================

Give me the Day 3 plan chronologically from beginning to end.

Number every major step.

For each step:

- tell me exactly what to do;
- tell me exactly which file/folder is involved;
- provide complete code when a file must be created;
- clearly say whether I should replace a whole file or only a specific section;
- give exact PowerShell commands;
- include verification immediately after important changes;
- do not assume I know where something belongs.

Do not jump ahead.

The goal is to finish Day 3 with a tested, locally persistent bilingual Profile Setup → Home flow that is ready to merge.
