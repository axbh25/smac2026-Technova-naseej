Assume that all of Day 4 steps are complete and verified for the Naseej Flutter app.

The project is part of the SMAC 2026 Project Development and the team name is Technova.

Project name:
Naseej

Local project path:
C:\dev\smac2026\naseej

GitHub repository:
axbh25/smac2026-Technova-naseej

Day 4 is complete.

The app currently supports:

Welcome
→ Profile Setup
→ locally saved profile
→ Home
→ Teach a Skill
→ locally saved Skill Draft
→ Home with saved draft
→ reopen and continue the draft

The app already supports:

- English and Arabic localization
- RTL
- persistent language selection
- persistent family profile
- persistent SkillDraft
- teacher and learner family roles
- four skill categories
- multiline skill explanation
- automated tests
- Android emulator verification
- local-only storage
- no account requirement
- no location requirement
- no backend
- no Firebase

For Day 5, I want to add optional speech-to-text voice input to the Teach a Skill explanation field.

The voice feature must assist the user with entering the explanation but must never make voice mandatory.

The typed TextField must remain available at all times, including when:

- microphone permission is denied
- speech recognition is unavailable
- speech recognition returns no match
- speech recognition encounters a network/service error
- the user simply prefers typing

I am a beginner, so give me extremely clear, chronological, baby-step instructions.

Tell me:

- exactly what file/folder to open
- exactly what file to create
- exactly what code to paste
- whether to replace an entire file or only a section
- exactly what PowerShell commands to run
- exactly what emulator checks to perform
- exactly what test output I should expect

Do not skip intermediate verification.

==================================================
DAY 5 MAIN GOAL
==================================================

By the end of Day 5, Naseej should support optional speech input in the Teach a Skill screen.

The user should be able to:

1. Open Teach a Skill from Home.
2. Scroll to a Voice Input section.
3. Tap a microphone/speech button.
4. Allow microphone permission when requested.
5. Speak in English or Arabic.
6. See recognized words appear inside the existing explanation TextField.
7. Preserve any text that was already typed before speech started.
8. Append recognized speech to existing typed text.
9. Stop listening manually.
10. Automatically stop listening when a final speech result is received.
11. Tap the explanation TextField and return to normal manual typing.
12. Save the SkillDraft normally after speech input.
13. Continue using the app normally when speech recognition is unavailable.
14. Continue typing when microphone permission is denied.
15. Keep all Day 2, Day 3, and Day 4 functionality working.

Voice input must be optional.

Do not store microphone recordings.

Do not upload microphone recordings.

Do not persist raw audio.

Only the resulting explanation text may become part of the existing locally stored SkillDraft.

==================================================
GIT WORKFLOW
==================================================

Start from the completed Day 4 work.

Use this Day 5 branch:

feature/day-05-speech-input

Give me exact Git commands as needed.

From Day 3 onward, Abdullah performs all VS Code, Flutter, PowerShell, and Git development.

Do not require Shoug to run VS Code, Flutter, PowerShell, or Git commands.

==================================================
DEPENDENCY
==================================================

Use an appropriate Flutter speech-to-text package for microphone speech recognition.

Use the speech_to_text package unless there is a strong technical reason not to.

Add only the dependency required for this Day 5 feature.

Give me the exact command to add it, such as:

flutter pub add speech_to_text

Then verify dependency installation with:

flutter pub get

Do not add unnecessary packages.

==================================================
ANDROID MICROPHONE PERMISSION
==================================================

Configure Android for speech recognition.

Update the appropriate AndroidManifest.xml file.

Add the microphone permission required for voice recognition.

Use:

android.permission.RECORD_AUDIO

Only add additional Android permissions if they are genuinely required by the chosen speech recognition implementation.

Do not request:

- location
- contacts
- camera
- storage
- phone
- SMS

unless the Day 5 feature technically requires them, which it should not.

Explain exactly where the permission belongs in AndroidManifest.xml.

==================================================
SPEECH ARCHITECTURE
==================================================

Do not place direct speech_to_text package calls throughout the UI.

Create a small speech abstraction so:

- production code can use real speech recognition
- tests can use a fake speech engine
- widget tests do not depend on a real microphone

Use a structure similar to:

lib/
└── core/
    └── speech/
        ├── speech_engine.dart
        ├── speech_to_text_engine.dart
        └── speech_controller.dart

Keep this architecture simple.

Do not over-engineer it.

==================================================
SPEECH ENGINE ABSTRACTION
==================================================

Create:

lib/core/speech/speech_engine.dart

Define the minimum abstraction needed by the application.

Include concepts such as:

SpeechLocale

with:

- localeId
- name

Define callbacks for:

- listening-state changes
- speech errors
- recognized words

The abstraction should expose operations equivalent to:

initialize(...)
locales()
startListening(...)
stopListening()
cancelListening()

and:

bool get isListening

The UI and SpeechController should depend on this abstraction rather than directly depending on speech_to_text internals.

==================================================
REAL SPEECH ENGINE
==================================================

Create:

lib/core/speech/speech_to_text_engine.dart

Implement SpeechEngine using the speech_to_text package.

It should:

- initialize speech recognition
- expose available locales
- begin listening
- return recognized words
- report whether a result is final
- expose listening state
- stop listening
- cancel listening
- forward relevant error codes

Do not store audio.

Do not send audio anywhere manually.

Use only the speech recognition functionality required by the package/platform.

==================================================
SPEECH CONTROLLER
==================================================

Create:

lib/core/speech/speech_controller.dart

Use ChangeNotifier.

SpeechController should:

- receive a SpeechEngine through its constructor
- initialize the engine only once
- track whether initialization has been attempted
- track whether speech is available
- track whether speech is currently listening
- expose the most recent speech error code
- start listening
- stop listening
- cancel listening
- notify listeners when speech state changes

Expose useful getters such as:

bool get initializationAttempted
bool get isAvailable
bool get isListening
String? get lastErrorCode

==================================================
LOCALE SELECTION
==================================================

Speech recognition must support the app's English and Arabic modes.

When the app locale is English, prefer an English recognition locale such as:

en_US

When the app locale is Arabic, prefer an Arabic locale such as:

ar_AE

Do not assume the exact locale always exists.

Use the engine's supported locale list and select the best matching available locale.

Use sensible fallback behavior when an exact locale is unavailable.

Tests should verify at least:

English → en_US

Arabic → ar_AE

using the fake speech engine.

==================================================
PROVIDE SPEECHCONTROLLER TO THE APP
==================================================

Update app startup so SpeechController is available through Provider.

The real app should have both:

AppController

and:

SpeechController

available to the widget tree.

Use MultiProvider where appropriate.

Construct SpeechController using the real SpeechToTextEngine.

Do not put speech recognition state inside AppController.

Keep application persistence state and speech-recognition state separate.

==================================================
LANGUAGE TOGGLE
==================================================

Update:

lib/core/widgets/language_toggle_button.dart

if needed so the button accepts:

enabled

The language control should normally remain enabled.

While speech recognition is actively listening, temporarily disable language switching.

This avoids changing recognition language in the middle of an active microphone session.

When recognition stops, language switching should become available again.

==================================================
VOICE INPUT UI COMPONENT
==================================================

Create a reusable component:

lib/features/skill/presentation/widgets/speech_input_card.dart

The component should clearly communicate speech state.

It should accept information such as:

- isListening
- initializationAttempted
- isAvailable
- title
- body
- buttonLabel
- privacyLabel
- errorMessage
- onPressed

Use the existing Naseej design system:

- AppColors
- AppSpacing
- existing typography
- rounded corners
- accessible button sizing

Use a microphone icon or similarly understandable Material icon.

Give the speech action button a stable test key:

speech_button

If speech recognition is permanently unavailable, show a clear fallback message.

Give the fallback message a stable test key:

speech_fallback_message

Do not hide the explanation TextField.

==================================================
VOICE UI STATES
==================================================

Support states such as:

Available / idle

Title concept:
Voice input

Body concept:
Speak instead of typing if you prefer.

Listening

Title concept:
Listening...

Body concept:
Speak naturally. Your words will appear below.

Unavailable / permission denied

Explain that voice input cannot currently be used.

Always tell the user that typing remains available.

Use localized strings.

==================================================
PRIVACY MESSAGE
==================================================

Show a small privacy/fallback notice in the Voice Input card.

The message should make clear that:

- voice input is optional
- users can type instead

Do not claim that speech recognition is fully offline unless that has actually been verified for the device/platform.

Do not claim audio is stored locally if the speech recognition platform itself does not guarantee that.

Do not store microphone recordings in Naseej.

==================================================
LOCALIZATION
==================================================

Update:

lib/l10n/app_en.arb
lib/l10n/app_ar.arb

Add all required Day 5 strings.

Include strings corresponding to concepts such as:

voiceInputSectionTitle
voiceInputTitle
voiceInputBody
voicePrivacyNotice
startSpeakingLabel
stopListeningLabel
speechListeningTitle
speechListeningBody
speechUnavailableTitle
typedFallbackLabel
speechPermissionDenied
speechNetworkError
speechNoMatch
speechGenericError

Provide accurate English and Arabic translations.

Arabic must work correctly in RTL.

Run:

flutter gen-l10n

after updating the ARB files.

==================================================
CONNECT VOICE INPUT TO TEACHSKILLSCREEN
==================================================

Open:

lib/features/skill/presentation/teach_skill_screen.dart

Integrate SpeechController with the existing explanation field.

Do not rewrite the existing SkillDraft form unnecessarily.

Keep:

- teacher information
- learner nickname
- learner role
- category
- explanation validation
- Save Draft behavior
- local persistence

Add:

import 'dart:async';

Import:

SpeechController

and:

SpeechInputCard

==================================================
SPEECH STATE IN TEACHSKILLSCREEN
==================================================

Inside _TeachSkillScreenState, keep the existing two text controllers.

Add speech-specific state similar to:

SpeechController? _speechController;
String _speechBaseText = '';

Use didChangeDependencies() to obtain the provided SpeechController once.

Do not create a new SpeechController every time build() runs.

==================================================
DISPOSE BEHAVIOR
==================================================

When TeachSkillScreen is disposed:

- cancel any active listening session
- dispose learner nickname TextEditingController
- dispose explanation TextEditingController
- call super.dispose()

Do not leave speech recognition active after navigating away.

Because cancelListening is asynchronous, use unawaited where appropriate.

==================================================
START / STOP SPEECH
==================================================

Create a helper method similar to:

_toggleSpeechInput()

Behavior:

If already listening:

- stop listening
- return

If not listening:

1. copy the current explanation text into _speechBaseText
2. start speech recognition using Localizations.localeOf(context)
3. receive recognized words through the callback
4. merge recognized words into the explanation TextField
5. update the cursor position
6. trigger a rebuild

==================================================
APPEND SPEECH TO EXISTING TEXT
==================================================

Voice recognition must not erase text the user typed before pressing the microphone button.

Example:

Existing typed text:

First I prepare the items.

Recognized speech:

Then I demonstrate the skill slowly.

Expected explanation:

First I prepare the items. Then I demonstrate the skill slowly.

Use the explanation text at the moment speech starts as the base text.

If there is no base text, use only recognized words.

If recognized words are empty, preserve the original text.

==================================================
EXPLANATION LENGTH LIMIT
==================================================

Continue respecting:

SkillDraft.maximumExplanationLength

If merged speech input exceeds the maximum length, truncate safely to the existing maximum.

Update the TextEditingController using TextEditingValue.

Place the cursor at the end of the resulting text.

Speech input must not bypass the same limits as manually typed input.

==================================================
SPEECH ERROR MESSAGES
==================================================

Create a helper method in TeachSkillScreen to convert speech engine error codes into localized messages.

Handle concepts such as:

permission / insufficient permission
→ speechPermissionDenied

network
→ speechNetworkError

no_match / timeout
→ speechNoMatch

anything else
→ speechGenericError

Do not expose raw implementation-specific error codes directly to normal users.

==================================================
STOP SPEECH BEFORE SAVING
==================================================

Inside _saveDraft():

stop speech recognition before writing the SkillDraft.

Do not leave the microphone active while navigating back to Home.

Avoid using BuildContext incorrectly across async gaps.

Capture needed controller references before awaiting asynchronous operations.

Use:

if (!mounted) {
  return;
}

when BuildContext-dependent UI code is used after an await.

flutter analyze must report no:

use_build_context_synchronously

issues.

==================================================
READ SPEECHCONTROLLER IN BUILD
==================================================

In build():

read AppLocalizations as before.

Watch SpeechController using Provider so the UI rebuilds as listening/error state changes.

Derive:

speechErrorMessage
permanentlyUnavailable
speechTitle
speechBody
speechButtonLabel

based on:

isListening
initializationAttempted
isAvailable
lastErrorCode

==================================================
DISABLE LANGUAGE SWITCHING DURING SPEECH
==================================================

Replace the static language action with:

LanguageToggleButton(
  enabled: !speechController.isListening,
)

The user should not be able to switch recognition language while actively listening.

==================================================
INSERT VOICE INPUT SECTION
==================================================

Place the Voice Input section before the existing explanation section.

Structure concept:

Voice input heading
SpeechInputCard
Explanation heading
Explanation TextField

Do not replace the TextField with voice input.

The TextField remains the actual editable explanation field.

==================================================
STOP SPEECH WHEN USER TAPS TEXT FIELD
==================================================

Inside the existing explanation TextField:

add an onTap callback.

If speech recognition is currently listening:

stopListening()

Use unawaited if necessary.

Keep the existing onChanged callback because it controls Save Draft validation.

==================================================
SAVE DRAFT STILL WORKS
==================================================

All existing validation remains unchanged.

Save Draft must still require:

- learner nickname
- learner role
- category
- explanation of at least the minimum length
- explanation no longer than the maximum length

Speech input is only another way to populate the same explanation controller.

Do not create a separate voice-only explanation value.

==================================================
FAKE SPEECH ENGINE
==================================================

Create:

test/support/fake_speech_engine.dart

Implement SpeechEngine.

The fake should allow tests to:

- control whether initialization succeeds
- expose supported locales
- track initialize call count
- track selected locale ID
- track whether it is listening
- emit recognized words
- emit final results
- emit errors
- stop/cancel listening

Default fake supported locales should include:

en_US
ar_AE

Do not use a real microphone in automated tests.

==================================================
SPEECH CONTROLLER UNIT TESTS
==================================================

Create:

test/speech_controller_test.dart

Test at least:

1. SpeechController initializes only once.
2. English selects en_US.
3. Arabic selects ar_AE.
4. Recognized words are forwarded.
5. Final results stop listening.
6. Unavailable initialization is exposed correctly.

Use FakeSpeechEngine.

When a callback parameter is deliberately unused, use:

_

rather than unnecessary names such as:

__

so flutter analyze reports no unnecessary_underscores lint.

==================================================
WIDGET TEST PROVIDERS
==================================================

Update:

test/widget_test.dart

Import:

SpeechController

and:

support/fake_speech_engine.dart

Update:

pumpReferenceApp()

so it accepts an optional:

SpeechController? speechController

Create an active controller:

speechController ??
SpeechController(
  FakeSpeechEngine(),
)

Dispose the active SpeechController during test teardown.

Wrap NaseejApp with MultiProvider containing:

ChangeNotifierProvider<AppController>.value

and:

ChangeNotifierProvider<SpeechController>.value

Do not use SingleChildWidget explicitly unless the required provider type import is also present.

A simple:

providers: [
  ...
]

is sufficient.

==================================================
TEST HELPER SPEECHCONTROLLER
==================================================

Update:

openTeachSkillScreen()

so it accepts:

SpeechController? speechController

Forward it to:

pumpReferenceApp(
  tester,
  storage: storage,
  speechController: speechController,
)

Existing tests that do not care about speech should continue working without explicitly passing a controller.

==================================================
VOICE WIDGET TEST 1
==================================================

Add a widget test:

voice input inserts recognized words into the editable field

Test process:

1. Create FakeSpeechEngine.
2. Create SpeechController(engine).
3. Open TeachSkillScreen using that controller.
4. Find speech_button.
5. ensureVisible.
6. Tap.
7. Verify fake engine is listening.
8. Emit recognized words:
   Explain how our family welcomes guests
9. Mark result final.
10. Pump.
11. Read explanation_field.
12. Verify the recognized text was inserted.
13. Verify the fake engine is no longer listening.

==================================================
VOICE WIDGET TEST 2
==================================================

Add a test:

voice input appends to existing typed text

Start with typed explanation:

First I prepare the items.

Then start speech recognition.

Emit:

Then I demonstrate the skill slowly.

Expected final explanation:

First I prepare the items. Then I demonstrate the skill slowly.

Verify speech does not erase the typed base text.

==================================================
VOICE WIDGET TEST 3
==================================================

Add a test:

microphone denial preserves the typed fallback

Use:

FakeSpeechEngine(
  initializeResult: false,
)

Open TeachSkillScreen.

Tap speech_button.

Verify:

speech_fallback_message

is visible.

Then manually type into:

explanation_field

Use a message such as:

Manual typing remains available after permission denial.

Verify the TextField contains the manually typed text.

This proves microphone failure does not block the core form.

==================================================
EXISTING TESTS
==================================================

Keep all existing Day 2–Day 4 tests.

Do not remove tests merely because Day 5 changes providers.

Existing tests should continue to cover:

- Welcome
- localization
- RTL
- Profile Setup
- profile persistence
- Home
- SkillDraft validation
- SkillDraft persistence
- restoring a saved draft
- reopening a saved draft
- accessibility

Keep the semantics-heavy accessibility test last.

==================================================
QUALITY CHECKS
==================================================

After implementation run:

flutter gen-l10n

dart format lib test

flutter analyze

flutter test

Do not continue until:

flutter analyze

reports:

No issues found!

and:

flutter test

reports:

All tests passed!

Fix analyzer info messages as well, including:

- unused imports
- unnecessary underscores
- use_build_context_synchronously

Do not ignore them for the final Day 5 result.

==================================================
ANDROID EMULATOR TEST
==================================================

Run Naseej on the Android emulator.

Verify English:

1. Open Home.
2. Open Teach a Skill.
3. Scroll to Voice Input.
4. Tap Start Speaking.
5. Handle microphone permission.
6. Speak an English sentence.
7. Confirm recognized words appear in explanation.
8. Confirm listening state changes visibly.
9. Confirm Stop Listening works.
10. Confirm tapping the explanation field stops voice recognition.
11. Confirm typing still works.
12. Complete the form.
13. Save Draft.
14. Confirm Home displays the saved explanation.

==================================================
ARABIC VOICE TEST
==================================================

Switch Naseej to Arabic.

Verify:

- Teach a Skill remains RTL
- Voice Input UI is Arabic
- microphone control is usable
- recognition requests an Arabic locale
- recognized Arabic text appears in the explanation field
- typed Arabic remains available
- language switching is disabled only while actively listening
- Save Draft still works

Do not assume Arabic recognition quality is identical on every emulator/device.

Document the actual observed behavior.

==================================================
PERMISSION-DENIED TEST
==================================================

Also manually verify the failure path if practical.

If microphone permission is denied:

- app must not crash
- Voice Input card should explain that voice is unavailable
- typed explanation must remain available
- Save Draft must still work using manually entered text

The application must remain useful without microphone access.

==================================================
PRIVACY REQUIREMENTS
==================================================

Naseej must not deliberately store:

- microphone recordings
- raw audio files
- speech session recordings

Do not add code that saves audio.

Do not add code that uploads recordings.

Only recognized text entered into the existing explanation field may be stored as part of SkillDraft.

Do not include real family voice recordings in repository evidence.

==================================================
SCREENSHOT / EVIDENCE
==================================================

Create appropriate Day 5 evidence under the existing documentation/testing structure.

Capture useful screenshots such as:

- English Voice Input idle state
- English listening state if reliably capturable
- explanation field containing recognized speech
- Arabic Voice Input screen
- unavailable/fallback state if practical

Do not commit actual microphone recordings.

Give me exact Windows PowerShell and ADB screenshot commands if screenshots are required.

Do not assume a PowerShell variable such as $adb exists unless it has first been defined.

==================================================
DAY 5 DOCUMENTATION
==================================================

Document the Day 5 implementation accurately.

Describe:

- optional voice input
- SpeechEngine abstraction
- SpeechToTextEngine
- SpeechController
- supported English/Arabic locale selection
- typed fallback
- permission-denied behavior
- no raw-audio persistence
- explanation text merging
- maximum explanation length enforcement
- automated speech tests
- emulator verification

Do not claim capabilities that were not actually tested.

==================================================
AI USAGE RECORD
==================================================

At the end of Day 5 create:

docs/ai-usage/prompts/2026-08-11-day-05-plan.md

Paste this exact Day 5 user prompt into that file.

Do not include:

- credentials
- passwords
- tokens
- personal family data
- microphone recordings
- private keys
- Firebase secrets

==================================================
GIT WORKFLOW AT THE END
==================================================

Before committing:

git status

Review changed files carefully.

Do not commit:

- credentials
- local secret files
- microphone recordings
- unrelated generated junk

Use clear Day 5 commit messages.

Push:

feature/day-05-speech-input

Prepare the Day 5 feature for a pull request into main.

Do not require Shoug to perform Git or VS Code work.

==================================================
DO NOT DO THESE THINGS
==================================================

Do not:

- make voice input mandatory
- remove the explanation TextField
- store raw microphone recordings
- commit voice recordings
- add Firebase
- add authentication
- add accounts
- add backend services
- add cloud speech APIs manually
- add location collection
- implement Learn a Skill yet
- break SkillDraft persistence
- break Profile persistence
- break locale persistence
- break Arabic RTL
- remove existing tests
- bypass explanation validation
- expose raw speech-engine errors directly to users
- hide real test failures
- add unnecessary architecture

==================================================
RESPONSE FORMAT
==================================================

Give me Day 5 chronologically from beginning to end.

Number every major step.

For each coding step:

- give the exact path
- say whether to create or edit the file
- say whether to replace the entire file or only part of it
- provide complete code when appropriate
- give exact PowerShell commands
- include verification immediately after important changes

Keep the instructions beginner-friendly.

Do not jump ahead.

The final Day 5 result should be a tested bilingual optional speech-to-text feature for the Teach a Skill explanation field, with manual typing always available and no microphone recordings stored by Naseej.