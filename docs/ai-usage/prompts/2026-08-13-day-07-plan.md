Assume that all of Day 6 steps are complete and the current Naseej Flutter app is working correctly.

I am continuing the SMAC 2026 Naseej project for Team Technova.

I am a complete beginner with Flutter, Firebase, Git, Android Studio, and mobile development, so guide me extremely carefully and do not skip steps.

Important working rules:
- Give me one clear step at a time.
- Tell me the exact file path whenever I need to create or edit a file.
- When a Dart file needs substantial changes, give me the complete corrected file rather than isolated snippets.
- Give PowerShell commands one at a time and tell me which folder I must be in before running them.
- Do not assume I know where files, Firebase settings, Android settings, or VS Code options are.
- Stop after each major step so I can show you the result before continuing.
- Do not make unrelated refactors.
- Preserve everything already working from Days 1–6.
- The app must remain bilingual English/Arabic with RTL support.
- The app must remain usable when AI, Firebase, the internet, microphone, or camera are unavailable.
- The locally saved skill draft must remain available even if the AI check fails.
- Do not send the user's skill draft, explanation, family information, nickname, role, context photo, speech content, or any other private family data to AI during Day 7.
- The Day 7 AI request must use only a fixed non-personal test prompt.
- Do not upload the Day 6 context photo to Firebase or Gemini.
- Do not add authentication, Firestore, Cloud Storage, Analytics, Crashlytics, or other Firebase products unless they are strictly required for today's task.
- Never put a raw Gemini API key, service-account key, Firebase login credential, App Check debug token, or any other secret in Dart source code, documentation, screenshots intended for GitHub, or Git commits.
- Do not enable Firebase App Check enforcement yet.
- Debug builds must use the App Check debug provider.
- Release builds must use Play Integrity.
- App Check debug tokens are private development secrets and must never be committed.
- From Day 3 onward, only I perform VS Code, Flutter, PowerShell, Firebase CLI, Git, and GitHub work. Do not tell Shoug to run coding or Git commands.

Current project:
- App: Naseej
- Team: Technova
- Repository: axbh25/smac2026-Technova-naseej
- Local project root: C:\dev\smac2026\naseej
- Android package: com.axbh25.naseej
- Flutter Android-first project
- Reference emulator/screen: Pixel 7 Pro, approximately 412 x 892 logical pixels
- Current architecture uses Provider / ChangeNotifier.
- AppController manages app/profile/draft state.
- SharedPreferences persists local app data.
- Speech-to-text is optional and typing remains available.
- Context photo capture is optional and stored privately in local app storage.
- Existing widget/unit tests must continue passing.

Day 7 goal:
Safely connect Naseej to Firebase and Firebase AI Logic, protect development access with Firebase App Check, and add a small AI Readiness feature on Home that proves the real Firebase AI connection works without sending any private family content.

The Day 7 feature is NOT the final AI teaching feature.

It is only a safe connectivity/readiness test.

I want Day 7 completed in a way that is simple enough for me to understand and defend during the SMAC judge Q&A.

Please guide me through the following Day 7 work in a safe order.

PART 1 — Create and prepare the Day 7 Git branch

Start from the latest main branch and create:

feature/day-07-ai-readiness

Give me the exact Git commands one at a time.

Do not commit or push until the relevant implementation/tests are verified.

PART 2 — Firebase project setup

Guide me through creating or selecting the Firebase project for Naseej.

Register the Android app with:

Package name:
com.axbh25.naseej

App nickname:
Naseej Android

Explain whether SHA-1 or SHA-256 is required at each stage instead of making me enter unnecessary fingerprints.

Use FlutterFire CLI for Flutter configuration rather than manually copying random Gradle snippets when FlutterFire can configure them correctly.

The project uses Kotlin DSL Gradle files (.gradle.kts), not Groovy.

Guide me through:
- Firebase CLI / FlutterFire CLI prerequisites if needed
- flutterfire configure
- selecting the existing Firebase project
- Android only
- selecting/registering com.axbh25.naseej
- generating firebase_options.dart
- verifying Android Gradle configuration
- verifying google-services.json without exposing sensitive values

PART 3 — Firebase dependencies

Add only the packages required for Day 7, including the appropriate current packages for:
- firebase_core
- firebase_app_check
- firebase_ai

Use package versions compatible with the current Flutter/Firebase SDK.

After dependency changes, run the appropriate Flutter commands and verify the project still analyzes.

PART 4 — Firebase initialization

Update the Flutter startup safely.

Firebase must be initialized before runApp using the generated Firebase options.

Set up Firebase App Check so:
- Android debug development uses AndroidProvider.debug
- Android release uses AndroidProvider.playIntegrity
- enforcement remains OFF for now

Preserve the existing providers and app initialization from Days 1–6.

Do not break:
- AppController
- SharedPreferences storage
- SpeechController
- optional camera/photo feature
- localization

If new providers are added, make sure both the real app and widget-test app receive the required providers.

PART 5 — Firebase App Check console setup

Guide me through registering the Naseej Android app in Firebase App Check.

Use Play Integrity for the registered Android production provider.

Explain how to obtain the required Android SHA-256 fingerprint using the project's debug signing report if Firebase requires it.

Do not ask me to expose keystore passwords.

For emulator development:
- use the App Check debug provider
- show me how to view the generated debug token using adb/logcat
- show me where to register that token in Firebase Console
- warn me never to commit or publicly share it
- do not enable enforcement yet

If App Check fails, the app must handle the failure gracefully instead of crashing.

PART 6 — Enable Firebase AI Logic

Enable Firebase AI Logic for the Firebase project.

Use the Gemini Developer API option for this project unless the Firebase console or SDK requirements clearly require otherwise.

Do not place a raw Gemini Developer API key inside the Flutter project.

Use Firebase AI Logic through the official Flutter Firebase SDK.

For the Day 7 readiness test, use:

gemini-2.5-flash-lite

The readiness request must contain only a fixed test instruction such as:

Return exactly NASEEJ_READY.

A suitable system instruction may tell the model that it is a private connectivity test and must return only:

NASEEJ_READY

No user-created family content is allowed in this request.

Specifically, DO NOT send:
- teacher nickname
- teacher role
- learner nickname
- learner role
- skill category
- explanation/draft text
- speech transcript
- context photo
- context photo path
- any personal family data

PART 7 — AI readiness domain/service architecture

Keep the implementation small and understandable.

Create an abstraction such as:

lib/core/ai/ai_readiness_service.dart

It should define the readiness result/failure types and a service contract.

Create the Firebase implementation such as:

lib/core/ai/firebase_ai_readiness_service.dart

The Firebase implementation should:
- create the FirebaseAI Gemini Developer API client
- create the Gemini model
- issue only the fixed readiness prompt
- accept NASEEJ_READY as success
- use a reasonable request timeout
- gracefully map expected failures

Useful failure categories should include cases such as:
- offline/timeout
- App Check rejected
- quota exceeded
- Firebase/API not configured
- service/API not enabled
- invalid AI response
- unknown failure

Never crash the Home screen because Firebase AI is unavailable.

PART 8 — AI readiness controller

Create:

lib/core/ai/ai_readiness_controller.dart

Use ChangeNotifier consistently with the existing Provider architecture.

Keep simple states such as:
- idle
- checking
- ready
- unavailable

The controller should expose:
- current status
- successful model name when ready
- failure information when unavailable
- checkReadiness()

Prevent accidental duplicate checks while one is already running.

Do not automatically send repeated AI requests in the background.

The user should explicitly initiate or retry the readiness check.

PART 9 — AI Readiness Home card

Create:

lib/features/home/presentation/widgets/ai_readiness_card.dart

Add the card to Home without removing or breaking the existing:
- profile summary
- saved/empty skill draft area
- Teach a Skill action
- Learn a Skill action
- language toggle

The card should have understandable states.

Idle:
- explain that an optional AI connection check is available
- button label similar to "Check AI connection"

Checking:
- show an accessible progress state
- prevent duplicate taps

Ready:
- clearly show that the AI connection is ready
- show the model label:
  Model: gemini-2.5-flash-lite
- make clear that the test did not send the user's draft or photo

Unavailable:
- do not crash
- give a short, useful fallback message
- allow "Check again"
- tell the user they can continue using their locally saved draft

Provide useful localized messages for specific failures where appropriate, for example:
- internet/service unreachable
- App Check/debug device not approved
- Firebase service not enabled/configured
- quota problem
- generic failure

The privacy note should clearly communicate that this connectivity check does NOT send draft text or the context photo.

Use existing Naseej design tokens:
- AppColors
- AppSpacing
- existing typography/theme
- rounded cards/buttons
- accessible contrast and touch sizes

Do not redesign the rest of Home.

PART 10 — Localization

Add all new AI Readiness strings to:

lib/l10n/app_en.arb
lib/l10n/app_ar.arb

Provide natural English and Arabic wording.

Arabic must display correctly in RTL.

Do not manually edit generated localization Dart files.

After editing the ARB files, regenerate localization using:

flutter gen-l10n

from the project root.

PART 11 — Figma / design documentation

If the Day 7 guide requires Figma updates, keep them small.

Add or document AI Readiness card states matching the implementation:
- Idle
- Checking
- Ready
- Unavailable

Include English and Arabic/RTL where appropriate.

Do not redesign unrelated Day 1–6 screens.

The implementation should remain the source of truth for functionality, while Figma remains the visual design/handoff reference.

PART 12 — Unit tests

Create a fake AI readiness service such as:

test/support/fake_ai_readiness_service.dart

Do not call the real Firebase/Gemini network from automated tests.

Add controller tests such as:
- successful AI connection -> ready
- offline/failure -> unavailable
- repeated check while already checking does not issue duplicate requests

The tests must be deterministic.

PART 13 — Widget tests

Update the existing widget test harness so any Home screen containing AiReadinessCard has an AiReadinessController provider.

Do not break the existing Day 1–6 tests.

Use the fake readiness service in widget tests.

If the new AI card makes the Home ListView taller, update scrolling in tests correctly rather than deleting assertions.

Keep the existing accessibility test last.

Add focused AI readiness widget coverage if useful without making the suite unnecessarily large.

PART 14 — Manual real-device/emulator readiness test

After automated tests pass, guide me through a real debug test.

Expected flow:

Idle
→ Checking
→ AI connection is ready

Expected model label:

Model: gemini-2.5-flash-lite

Verify:
- the request is real, not a fake test service
- the app remains responsive
- retry works
- App Check works in debug mode
- no draft text is sent
- no context photo is sent
- local draft remains available when AI is unavailable

If App Check rejects the emulator, guide me through registering the private debug token rather than disabling App Check.

If networking fails, diagnose it without weakening application security.

PART 15 — Quality checks

Before Git work, run from:

C:\dev\smac2026\naseej

the appropriate checks, including:

flutter gen-l10n
dart format lib test
flutter analyze
flutter test

Fix all actual analyzer/test failures.

Warnings from external tooling should be explained separately if they do not represent a Day 7 application failure.

Do not accept a failing analyzer or failing tests.

PART 16 — Documentation

Update the README only where needed to document the Day 7 architecture/setup at a high level.

Document:
- Firebase Core
- Firebase App Check
- Firebase AI Logic
- AI readiness feature
- fixed non-personal readiness prompt
- privacy behavior
- local-first/offline fallback
- emulator debug setup at a safe high level

Do NOT put any real secret/token/key into README or other documentation.

Update the project AI usage log/documentation so Day 7 clearly records:
- AI was used as a planning/debugging assistant
- the human implemented, ran, reviewed, and tested the changes
- the final app behavior was verified by the team

PART 17 — Store this exact Day 7 prompt

Create:

docs/ai-usage/prompts/2026-08-13-day-07-plan.md

Paste this exact user prompt into that file.

Do not include:
- Firebase login credentials
- App Check debug token
- raw Gemini key
- service-account key
- private family data
- personal context photos
- machine-specific secrets

It is okay to include non-secret project identifiers such as:
- repository name
- Android package name
- Firebase project name/ID if needed for reproducible documentation

PART 18 — Git and GitHub

Only after:
- Firebase configuration is working
- AI readiness succeeds or its graceful fallback is verified
- localization is generated
- flutter analyze passes
- flutter test passes
- documentation is safe and contains no secrets

show me how to review:

git status
git diff
git diff --cached

Make sure secrets are not staged.

Then create meaningful Day 7 commit(s), for example describing:
- Firebase/App Check setup
- AI readiness service/UI
- tests/documentation

Use the branch:

feature/day-07-ai-readiness

Push the branch and guide me through opening the Day 7 pull request.

Do not merge automatically.

FINAL VERIFICATION

At the end of Day 7, verify that:
1. Naseej still launches on Android.
2. English and Arabic still work.
3. RTL still works.
4. Existing profile persistence still works.
5. Existing skill draft persistence still works.
6. Optional speech input still works/falls back to typing.
7. Optional context photo still works/falls back safely.
8. Firebase initializes correctly.
9. App Check is configured for debug and release appropriately.
10. App Check enforcement has NOT been enabled prematurely.
11. AI readiness can make a real Firebase AI Logic request.
12. The AI readiness request sends only a fixed non-personal prompt.
13. No draft text or context photo is sent to AI.
14. AI/network/App Check failures do not block use of the locally saved draft.
15. flutter analyze passes.
16. flutter test passes.
17. No secret token/key/credential is committed.
18. The Day 7 prompt is stored in docs/ai-usage/prompts/2026-08-13-day-07-plan.md.
19. The Day 7 work exists on feature/day-07-ai-readiness.
20. I can explain the architecture and privacy decisions myself during the SMAC judge Q&A.

Do not jump ahead to implementing the final AI teaching/coaching feature. Day 7 is only Firebase setup, App Check protection, and the privacy-safe AI readiness proof.